# Fastiplayer identity migration

Product identity is Fastiplayer; binary `fastiplayer`, packages `fastiplayer-config` (directory `crates/config`) and `fastiplayer-settings` (directory `crates/fastiplayer-settings`). All versions and upstream dependency versions are preserved. No automatic legacy aliases. Environment prefix `FASTIPLAYER_`, MPRIS `org.mpris.MediaPlayer2.fastiplayer`, track paths `/com/fastiplayer/Track`, HTTP identities and patched Symphonia consumers use the same new identity.

Config owns `~/.config/fastiplayer` and `fastiplayer.instance.lock`; startup still locks before settings writes and playlist restoration. Owner profile settings and queue were copied with a timestamped private backup; old lock/resume omitted because the fingerprint domain changed. Original source profile retained as recovery copy. XSPF exports/imports `urn:fastiplayer:xspf:playlist-extension:1`; foreign namespaces remain unknown and standard tracks survive.

Coverage v2 migration lives in `scripts/coverage_identity_migration.py`. Registered logical hashes pin old/new baseline, policies and ledgers; deterministic reconstruction separately enforces complete file/coordinate/stable/domain bijection and unchanged counters/settings/ledger. CLI optional group is `--identity-migrations --previous-policy --proposed-policy`; CI passes all three. Actual CLI adversarial tests: `scripts/tests/test_coverage_identity_migration.py`. New baseline logical hash `sha256:e069de775ef63db0c513b55126745912bd1dbe3e87e81348748de19a0531cde1`; old measurements are transferred, not requalified from a lower new run. Runtime qualification outcomes are maintained in `mem:global/fastiplayer-rename-verification-2026-09-05` so status-only updates do not change the qualified source tree.

Current demo `docs/assets/fastiplayer-main.png` is an unedited real 1280×720 window on AMD Ryzen 7 7840HS/Radeon 780M, KDE Wayland, software AV1 at 00:24 and muted active 48kHz six-channel audio. Capture hashes/provenance/BBB attribution are in docs/assets/README.md. Old designs and old current-tree screenshot deleted; T480s numbers and original revision hashes preserved and explicitly labeled historical with normalized names. LOGO.png unchanged.

Repository identity is `Bogdan7c/fastiplayer`; the agreed final local project location is `<PROJECTS_DIR>/fastiplayer`. Codex project/hooks/active workspace bindings and Serena project registration follow that location. Exact move/activation and final GitHub gate results are recorded in `mem:global/fastiplayer-rename-verification-2026-09-05`. See `mem:testing/coverage` for qualification policy.