# Video Decoder Stream Boundary

- `video-core::VideoDecoderThreadHandle` is the neutral decoder control boundary for playback-facing code. It exposes packet send/flush/release/receive plus stream lifecycle methods: `configure_stream(VideoStreamDecodeConfig)`, `clear_stream()`, `begin_end_of_stream_drain(generation)`, and `end_of_stream_drain_state()`.
- `VideoStreamDecodeConfig` carries the selected video stream contract owned by `player-core`: `track_id`, codec/profile, bit depth, chroma, coded dimensions, surface format, expected decoded memory contract, optional `codec_private`, and optional packetization metadata. H.264 packetization is represented as `VideoStreamPacketization::H264(H264Packetization::{AnnexB, AvccLengthPrefixed { nal_length_size }})` once codec metadata proves it.
- Configure results must keep distinct states: absent decoder, same-stream no-op, applied/cleared config, unsupported config with typed rejection, backend fatal error, and control-channel backpressure. Callers must not collapse these into `bool` when selection/retry/fatal policy depends on the reason.
- EOF/DPB drain is separate from seek `flush`. `begin_end_of_stream_drain(generation)` starts decoder tail draining for the current generation, frames still exit through normal `try_recv_frame`, and `VideoDecoderEndOfStreamDrainState` reports `Idle`, `Draining`, `Drained`, or `Fatal`. This API must not advance seek generation, clear pending packets, or read new demux packets.
- `DecodePacket` includes both presentation and decode-order timestamps: `pts` remains presentation timestamp, while `dts` and `track_dts` preserve existing demux metadata for codecs such as H.264 that need decode order. Do not invent a second timestamp contract when `media-core::Packet` already provides these fields.
- Current production VAAPI backend still only accepts the implemented VP9 adapter at configure time. H.264 config may be built and tested through fake backends, but production VAAPI H.264 decode should stay `UnsupportedCodec` until the H.264 adapter, packet conversion, DPB drain, and zero-copy export path are implemented.