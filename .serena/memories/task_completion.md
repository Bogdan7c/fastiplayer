# Task Completion

- Before a code task, call Serena `initial_instructions` once per session, activate the current Rustiplayer checkout, read `mem:core` and relevant memories, and correct clearly stale memories before edits. Check Context7 before edits or new code; use an official primary source and record the limitation if relevant documentation is unavailable. Inspect symbols/references/implementations/diagnostics before boundary/API changes. State owners, boundaries, invariants and functional tests before implementation; stop for important undecided architecture/product choices.
- Follow `AGENTS.md`, including plain-language explanations and the owner's Russian production-code documentation and comments for key logic, non-obvious decisions and invariants (not every line; owner clarification during S05). Check branch/HEAD/status and preserve unrelated user changes.
- Run the narrowest relevant functional tests first with `cargo test -p <crate> --locked`; include neighboring consumers when contracts change. Successful playback tests must reach render/presentation or the appropriate audio consumer, not stop at a helper or packet read. Add fake/absent/error/accounting/foreign-state checks for new boundaries.
- Use `mem:testing/sandbox_policy` for validation execution. Media/GUI/hardware acceptance is optional and must use explicitly selected inputs; never assume owner-local filenames. See `mem:testing/media-fixtures` and `mem:testing/playback-smoke`.
- `scripts/ci-checks.sh` is the authoritative blocking runner; inspect its current help and implementation rather than copying an old command inventory. `scripts/pre-pr-checks.sh` delegates to `scripts/ci-checks.sh all`: format-guardrails, dependency-patches, dependencies, strict all-targets/all-features Clippy and rustdoc, hermetic workspace tests, app-no-default-features, and MSRV. Run the relevant gates for a local change; run the full runner for broad pre-PR qualification.
- Seven local `[replace]` patch crates currently have standalone manifests/lockfiles, intentionally outside workspace membership. The inventory is `DEPENDENCY_PATCH_MANIFESTS` in `scripts/ci-checks.sh` and the dependency-patch policy. Use `cargo test --manifest-path <patch>/Cargo.toml --locked` for a focused patch, run each standalone manifest suite when validating all patches, then `scripts/ci-checks.sh dependency-patches` for inventory/integration. The `all` runner additionally invokes the seven direct patch suites. Do not add patches to workspace members or inherit first-party metadata/lints.
- Primary toolchain is Rust 1.96.0 (`rust-toolchain.toml`); use `cargo +1.96.0 check --workspace --locked` for public types/features/dependency/multiple-crate changes. Toolchain, workspace dependency or lock-graph changes also require `scripts/ci-checks.sh msrv` (Rust 1.92.0). Keep command knowledge aligned with the toolchain policy.
- Run `cargo fmt --all --check` for Rust changes and format if necessary. Use `scripts/ci-checks.sh clippy` for strict broad quality validation; explain skipped gates for small isolated changes.
- For documentation/tooling-only changes, validate the edited formats, links, command contracts and actual tooling behavior; do not claim playback or coverage evidence from static checks. Codex, Context7, Serena, hooks and the optional read-only tunnel must not become build/test prerequisites.
- Sonar runs only on explicit request: `SONAR_USER_HOME=/tmp/rustiplayer-sonar-user-home scripts/sonar-local-analysis.sh`, with `SONAR_TOKEN` only in the environment. Wait for the CE background task, group findings, fix root causes, and rerun relevant tests/Clippy/Sonar.
- Before final response, self-review the diff for unintended boundaries/behavior changes, cosmetic scope creep, ignored errors and tests that only assert symptoms. Assess architectural boundaries, APIs, workflow, commands, invariants, limitations and key test locations; update affected memories through Serena `write_memory`/`edit_memory` if changed, otherwise explicitly report that updates were unnecessary.
- After memory edits, run `serena memories check .` and inspect its report: it always exits 0, even with stale references. For Serena configuration changes, verify activation in a fresh MCP process; an already-running server may cache the old project name. `serena project health-check .` is optional comprehensive tooling validation, not a Cargo gate.
- Public memories use `<REPO_ROOT>`, `<MEDIA_DIR>` and `<PRIVATE_BACKUP_DIR>` placeholders, descriptive media properties and relative project commands. Never store personal paths, private filenames, credential values or private harness configuration. Preserve technically useful and Russian-language knowledge.
- Final response states changes, evidence, limitations and commands not run with reasons. Follow the session's separate stop-gates before any remote publication or destructive operation.
