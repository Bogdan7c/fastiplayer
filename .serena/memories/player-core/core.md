# Player Core

- `player-core` root modules from `src/lib.rs`: `audio_boundary`, `command`, `decoder_boundary`, `diagnostics`, `error`, `event`, `media_opening`, `pipeline`, `render_lease_bridge`, `seek_state`, `session`, `snapshot`, `state`, `worker`, `worker_scheduler`. Tick orchestration lives under the session boundary at `src/session/tick/`; `mod.rs` owns the main `PlayerSession::tick` loop and position update, `types.rs` owns public tick DTO/config/result types, `tests.rs` owns tick module tests, and child modules own demux admission, presentation scheduling, decoder I/O, and worker wakeup planning. Root `lib.rs` re-exports the public tick types so external API paths stay unchanged.
- Public/internal player boundary exports include `PlayerCommand`, `PlayerCommandSender`, `PlayerSnapshot`, `PlaybackState`, `PlayerEvent`, `PlayerWorkerEvent`, `PlayerWorker`, `PlayerWorkerConfig`, `PlayerRenderError`, seek types, runtime settings apply contracts (`PlayerRuntimeSettingsUpdate`, `PlayerRuntimeApplyReport`, typed setting/group/outcome ids, `PlayerRuntimeApplyError`), and `video-backend-api` re-exports such as `StartedVideoBackend`/`PresentFrameResourceProviderHandle`. Present-frame lease vocabulary lives in `video-present-core`; `player-core` returns `video_present_core::VideoFrameLease` from `PlayerWorker::try_acquire_present_frame` but does not re-export old `PresentFrameLease`/`PlayerPresentFrame` aliases. The factory trait lives in `video-backend-api`; concrete backend startup/materialization stays outside the playback crate. Since 2026-06-25, current playback mute is an explicit player-core command boundary: `PlayerCommand::ToggleMute { fallback_volume }` is handled by `PlayerSession`, which privately owns `last_nonzero_volume`. `SetVolume(v > EPSILON)` updates that remembered value, `SetVolume(0)` mutes without clearing it, and unmute restores remembered volume, then a valid fallback default volume, then `1.0`; `audio.volume` config remains startup/new-media default only.
- `PlayerWorker` owns `PlayerSession` on a worker thread. UI sends commands and reads latest snapshot/events; UI must not call `PlayerSession::tick()` directly. Settings committed player updates must use the settings-specific request/reply boundary (`PlayerCommandSender::apply_runtime_settings` or `PlayerWorker::apply_runtime_settings`) so callers receive the real `PlayerRuntimeApplyReport` rather than only command-queue acceptance; queue `Backpressure` and worker `Disconnected` are distinct typed errors.
- `PreparedMedia` is the media-opening boundary. Shell/service opens concrete demuxers and hands `Box<dyn media_core::Demuxer + Send>` plus tracks/duration/seekability to worker.
- `PlayerSession` fields are private; sibling modules outside `session` must use session-owned boundary methods instead of reading session storage. `PlaybackPipeline` owns runtime session slots but is not a data bag. Fields are private; cross-module access inside the session boundary goes through intent methods.
- `PlaybackPipeline` keeps the owner struct and shared DTO/helper types in `crates/player-core/src/pipeline.rs`, while inherent impl methods are split into private child modules: `pipeline/audio.rs` for audio decoder/output/clock/EOF-drain boundaries, `pipeline/media_slots.rs` for opened media/demux/track selection/seek generation/pending packet and present queue slots, `pipeline/video_decoder.rs` for decoder thread/send/receive/backpressure/activity boundaries, `pipeline/render_resources.rs` for render lease/deferred texture release accounting, and `pipeline/tests.rs` for focused pipeline tests. These are private modules only; root re-exports/public API paths stay unchanged.
- Key pipeline domains: opened media/demux slots, selected tracks/active video requirement plus expected active `VideoFrameContract`, seek generation/timing, audio decoder/output/clock, packet queues, present queue, present frame/fallback frame, decoder I/O/accounting, render generation/lease accounting.
- Accelerated video overload recovery is also pipeline-owned (`pipeline/video_backlog_recovery.rs`), not scheduler-owned storage. At rate `>1x`, audio-priority admission may retain the existing decoder-facing FIFO while staging new compressed AV1/VP9 packets until a proven selected-track/current-generation keyframe. That keyframe atomically replaces only not-yet-sent pending/staged packets; no decoder flush or generation change is hidden in this boundary. EOF, packet/byte limit, and successful downshift to `<=1x` restore staged continuation after the old FIFO; rejected rate change preserves scan unchanged; seek/media/track/generation/decoder replacement discard staging as real discontinuities. Pause and accelerated-to-accelerated changes preserve it. Staging is bounded by 512 packets and 32 MiB by default, exposes current packet/byte depth through `PipelineQueueDepthSnapshot`, and never moves rate/clock ownership into `frame-server-core` or concrete decoder crates. H.264/H.265/VP8 stay on ordinary FIFO until a safe no-flush random-access contract exists.
- Keep `clear_pending_packets_for_seek`, decoder resets, clock resets, render lease accounting, and texture/resource releases as explicit ownership/lifecycle decisions; do not bury them in unrelated helpers.
- Audio decode/output/tempo creation is a neutral `audio-core` boundary: `AudioDecoderConfig`, `EncodedAudioPacket`, packet timing, `AudioDecoderFactory`, `AudioOutputFactory`, `AudioTempoProcessorFactory`, `AudioTempoProcessor`, `PlayerAudioOutput`, `AudioOutputWriteReport`/typed input-vs-stream frame counts, and `PlayerAudioClock` live outside `player-core`; `player-core` re-exports the contracts, stores only trait objects/config values, and uses internal missing factories for default/test constructors. `PlaybackPipeline` owns the active tempo processor, bounded 600 ms direct-PCM warmup history and piecewise output-clock <-> media-time mapping; `app-egui` wires the concrete runtime `audio-signalsmith` factory. `player-core` must not depend directly on `audio-signalsmith`, `signalsmith-stretch`, `audio-timestretch` or `timestretch`. Scheduler uses player-owned deadline intent methods rather than inferring clock semantics through `has_audio_clock`; CPAL types remain in `crates/audio`. `DecodedAudioPacket::Pcm` переносит единый `AudioOutputSpec` (sample rate + neutral channel layout), а `PlaybackPipeline` хранит spec, принятый активным output-ом. Mid-stream layout change отвергается до write/tempo mutation даже при том же channel count; runtime packet path переводит такую несовместимость в fatal, а не отключает audio ради video-only fallback. Passthrough warmup history также сопоставляется по полному spec. Full rate-change, pause, failure and timing invariants are in `mem:player-core/playback-rate-contract-s32`, while the neutral DSP/output lifecycle is in `mem:audio/core`.
- `AudioOutputFactory` stays `Send + Sync` because it is passed into the worker, but `PlayerAudioOutput` is intentionally thread-local and does not require `Send`: production CPAL output is created inside `player-worker`, then written directly through the trait object to avoid proxy-thread/mpsc copies in the PCM refill hot path. `PlayerAudioClock` remains `Send + Sync` for CPAL callback/worker sharing.
- Video presentation lease/lookup vocabulary is renderer-neutral in `video-present-core`: `VideoFrameLease` exposes decoded-frame accessors, `VideoPresentFrameResourceDescriptor`, `VideoPresentFrameResourceLookup`, provider-backed lookup, diagnostics reporting, `VideoPresentFrameIdentity` (`render_generation`, `resource_handle`, `decoded_generation`, `pts`), and `VideoFrameReleaseSink`. `player-core::RenderLeaseBridge` only adapts this lease to player-owned release accounting/diagnostics sinks. It must not create, return, or re-export `wgpu::TextureView`, WGPU-named public API, or old player-core lease aliases.
- Scrub event/visual override boundary remains for retained frame-server paths: `PlayerWorkerEvent::Scrub(frame_server_core::ScrubEvent)` carries normalized scrub events, and `PlayerWorker::try_acquire_scrub_visual_override_frame()` reads the separate render-lease handoff slot from playback `try_acquire_present_frame`. Playback latest-frame cache/slot remains separate. Since 2026-07-03, prepared-hover promotion and hover working-set ownership are removed; SeekLanding/live scrub use the cold reused-decoder path and the remaining normalized scrub events without hover prepared-frame reuse.\n- OBSOLETE since 2026-07-03: the S18/S25 prepared-hover handoff/controller/snapshot path was removed. `PlayerTimelineHoverPrepareHandoff`, `PlayerSnapshot.timeline_hover_prepare`, hover stream decode context, prepared working-set promotion, and app-side hover executor ownership must not be reintroduced. See `mem:archive/player-core/timeline-hover-prepare-s25` only as an obsolete marker.\n- Live timeline scrub is wired through the public `BeginScrub`/`PreviewScrub`/`EndScrub` command family without adding a separate real-preview command. These variants may carry optional `frame_server_core::LiveScrubDiagnostics`; ordinary compatibility callers should use `PlayerCommand::{begin_scrub, preview_scrub, end_scrub}`, while real live-scrub UI uses `begin_live_scrub`, `preview_live_scrub`, and `end_live_scrub`. `PreviewScrub` starts a reused-decoder live `ScrubRequestKind::LiveScrub` route for selected-video pipelines, while `UpdateScrub` remains lightweight. Live scrub UX (2026-07-03): LiveScrub route показывает preroll «прокатом» — `active_seek_presents_preroll_progressively()` отключает decoder output floor и player-side drop, а `present_live_scrub_preroll_roll` в presentation_scheduler показывает новейший queued кадр latest-wins до landing (после показа landing прокат выключается, чтобы resume decode-ahead не уводил картинку дальше цели). Цель вперёд в пределах 3s (`LIVE_SCRUB_FORWARD_EXTENSION_MAX`) не суперседит, а расширяет активный проход (`try_extend_active_live_scrub_landing_forward`): без flush/demux seek, тот же generation, сброс только audio runtime (`reset_audio_runtime_for_seek_landing`) + clock base + рестарт trace для повторного `SeekTargetFramePresented`. Назад/дальше капа/EOF-drain/после запрошенного commit — cold route как раньше. Focused tests: session/tests/scrub.rs (`live_scrub_preroll_frames_present_progressively...`, `live_scrub_roll_presents_newest...`, `live_scrub_forward_target_extends...`, `live_scrub_backward_or_far_forward...`). `SeekRuntimeState` distinguishes one-shot SeekLanding from `LiveScrub { commit_requested, diagnostics }`: live preview can demux/decode and publish exact preview while public state stays `Scrubbing`, but commit cannot complete until `EndScrub` requests it and resets the commit timeout start. Live diagnostics attach to normalized `ScrubEvent` diagnostics and must not affect lifecycle, release ownership, or fatal/error policy. Since 2026-07-03, live scrub no longer reuses prepared-hover entries because that subsystem is removed.\n- Rendered texture release path is part of render lease accounting: render bridge reads stable present-frame identity through `PlayerSession::current_present_frame_identity()` and creates `video_present_core::VideoFrameLease` values through `lease_present_video_frame()`, without accessing `PlayerSession::pipeline` directly. Only a `VideoFrameLease` marked as submitted to renderer may later release through the original `PresentFrameResourceProviderHandle`; an acquired but unsubmitted lease must not be forced into GPU-completion release and can use the normal decoder release path.
- WGPU materialization belongs outside `player-core`: `app-egui` selects a concrete app-owned `VideoPipelinePlan` from committed video config plus the last `SystemCapabilities` snapshot, composes concrete backend startup, keeps the `render-wgpu::WgpuFrameTextureViewMaterializer`, and sends only `StartedVideoBackend` / playback-facing decoder handle to `PlayerWorker`. Runtime decoder queue/channel/pool reconfigure follows the same boundary: `app-egui` rebuilds the concrete backend through `AppState`, then `player-core` records the accepted `PlayerVideoDecoderThreadConfig` through `PlayerWorker::apply_runtime_settings`; no `app-egui` code mutates `PlayerSession`, `PlaybackPipeline`, decoder queues, or decoder internals directly.
- Backend startup/resource-provider boundary lives in `video-backend-api`; concrete video backend crates, including `video-vaapi`, must not depend on `player-core` for adapter contracts.
- Closed dependency debt: `player-core` depends on `audio-core`, not `audio`; decoder creation goes through `AudioDecoderFactory` supplied by composition, output creation through `AudioOutputFactory`, and unsupported-codec typed errors remain recoverable without direct Symphonia/Opus/CPAL knowledge. The old direct `player-core -> wgpu` texture-view bridge is closed and must stay closed.
- S13B/S14 introduced an intentional internal dependency `player-core -> frame-server-core` for scrub transaction execution. `crates/player-core/src/session/scrub_driver.rs` owns the player-side driver over `ScrubStateMachine`: clear old Accurate floor -> flush existing decoder -> bind nested scrub generation -> clear pending queues -> compute player-owned decode-point-before -> seek the existing demuxer -> feed/drain marker through existing boundaries. Since S17A, ordinary video `PlayerCommand::Seek` and `EndScrub` release enter this one-shot SeekLanding route; `SeekRuntimeState` tracks pending/active SeekLanding markers, public state stays `PlaybackState::Scrubbing` while cold exact decode is pending, and the existing `SeekCommitState` remains the final commit candidate decoded by the reused playback decoder. `ScrubGenerationToken { playback_generation, scrub_generation }` is validated against the owner playback generation before lifecycle intents run; new/superseding targets advance only the nested scrub generation, while finish/cancel/exit advances playback generation to make in-flight scrub output stale. Supersede cancel may release the old target only when playback generation still matches; stale playback-generation mismatch fails closed before lifecycle side effects. The driver must not create a second decoder/session/demuxer or move real decoder I/O ownership out of `session/tick/video_decoder_io.rs`. Audio resume timeout budget policy is player-core-owned (`min(500 ms, output buffer + callback/device period + 25 ms)`, fallback 500 ms for unknown/invalid timing) and must not be added to `frame-server-core` or `audio-core` without a separate boundary decision. Focused tests live in `crates/player-core/src/session/tests/scrub_driver.rs` and `crates/player-core/src/session/tests/scrub.rs`; keep `frame-server-core` free of `player-core` dependencies.
- Guardrails protect the player/frame-server reused-decoder boundary: `player-core -> frame-server-core` remains allowed, but `frame-server-core -> player-core` is forbidden. `session/scrub_driver.rs` and `session/prepared_seek.rs` must not create a second `PlayerSession`/`PlayerWorker`/backend factory/session for main-video SeekLanding/LiveScrub. Since 2026-07-03, prepared-hover branch promotion and app-side HoverPreview borrow paths are removed; future player work must not reintroduce hover-specific ownership, working sets, or overlay paths. Keep lifecycle decisions on the player-owned transaction boundary and update `scripts/check-refactor-guardrails.py` plus focused tests intentionally.\n- S13C made `PlaybackState::Scrubbing` a public observable state for the current lightweight scrub path. It is a frozen/non-playback-active state: do not add it to normal demux/present playback predicates. `BeginScrub`/`UpdateScrub`/`PreviewScrub` publish Scrubbing and remember the confirmed pre-scrub state; `EndScrub` with a latest target goes directly to final seek/`Seeking`, while release without a target restores the confirmed state and resumes audio/clock only when that state was `Playing`. External `Play`/`Pause`/`TogglePlayback` while simple Scrubbing cancel the active scrub first without hidden commit. Since S17A, ordinary video `Seek` while Scrubbing cancels first, then starts the unified one-shot SeekLanding route instead of the old independent final-seek route. Desktop/MPRIS maps Scrubbing as paused; app UI/telemetry handle it explicitly.
- Closed dependency violations must stay closed: no direct `player-core -> symphonia-demux/webm-demux` opening path, no direct `player-core -> video-vaapi` concrete backend internals, and no new direct `player-core -> render-wgpu` edge.
- Pipeline tests already cover many boundary methods for absent resource/no-op/error/accounting behavior; extend those focused tests when adding new boundary methods. Worker implementation is split under `crates/player-core/src/worker/`: `sender.rs` owns `impl PlayerCommandSender`, `handle.rs` owns public `impl PlayerWorker` and `Drop`, `runtime_wait.rs` owns worker wait/wakeup and decoder-activity runtime methods, `runtime_commands.rs` owns command handling and runtime settings apply methods, `runtime_publish.rs` owns diagnostics summary plus tick/event/snapshot publishing, and `tests.rs` owns worker tests. Runtime settings focused tests live in `crates/player-core/src/worker/tests.rs` for accepted tick apply, invalid/unsupported reports, default-volume non-mutation, decoder-thread config acceptance after app-owned controlled rebuild, request/reply response, and backpressure/disconnected distinction; `session/tests/playback.rs` keeps `ReloadConfig` compatibility-event behavior covered.
- Session diagnostics boundary lives in `src/session/diagnostics_sink.rs`: `PlayerSession` owns private `PlaybackDiagnostics`; `record_*`, diagnostics snapshot/log summary, and queue-depth attribution live behind session methods. `PlaybackDiagnosticsSnapshot.frame_server_scrub` nests the neutral `frame_server_core::ScrubDiagnosticsSnapshot` inside the existing `PlayerSnapshot.diagnostics` read model; no separate UI/public channel is used. `PlayerSession` records normalized scrub event diagnostics when publishing scrub events. Removed hover/prepared-frame diagnostics must not be reintroduced without a new explicit owner/boundary decision. `snapshot_builder` must use `PlayerSession::diagnostics_snapshot()` instead of reading the diagnostics field. The sink may read pipeline queue/resource snapshots but must not own or mutate pipeline state.\n- Active Accurate seek preroll diagnostics are stored in `seek_state::SeekTraceState` and surfaced through `ActiveSeekDiagnosticsSnapshot::accurate_preroll` plus worker stall logs. `seek_transaction` decides when Accurate skip/preroll semantics are active; tick modules only report observed events/counters (demux event kind, skipped audio preroll, total seek video packets sent, pre-target video packets sent, target-or-after video packets sent before landing, decoded pre-target frame drops, decoder/video admission backpressure). `SeekMode::KeyframeBefore` must leave this Accurate diagnostics snapshot inactive.
- Video drop diagnostics keep raw and seek-aware totals separate: `VideoDropCountersSnapshot::total` is all typed removals, `playback_or_render` is non-seek playback/render/pause reasons, and `seek_discard` aggregates `SeekPreroll` plus `StaleGeneration`; UI/user-visible telemetry must not treat raw `total` as ordinary playback drops.
- Session media lifecycle boundary lives in `src/session/media_lifecycle.rs`: it contains `load_demuxer*`, `load_prepared_media*`, `begin_media_open`, `fail_media_open_with_error`, `install_prepared_media`, `mark_media_opened`, `reset_media_state`, demux track-list update handling, and `MediaLifecycleState { pending_autoplay }`. `PreparedMedia` remains the player-core opening boundary; do not add direct player-core dependencies on concrete demux crates. Failed prepared-open errors are recoverable when no active `Opening` request matches, so an existing playback pipeline is not reset or forced to `Failed`; failures of the currently active opening request remain fatal for that opening.
- Session capability selection boundary lives in `src/session/capability_selection.rs`: it owns capability report publication, video requirement validation/refinement, default video selection, explicit `SelectVideoTrack(TrackId)` validation, active requirement refinement after packet probes, stream decode config construction, and codec metadata helpers. `PlayerSession::capabilities` stays private session state; selected video tracks must receive a fresh matching `VideoDecodeRequirement` and, when a decoder is installed, an accepted `VideoStreamDecodeConfig` before `PlaybackPipeline` mutation, or the command is rejected with a typed `PlayerError`. H.264 config adds `VideoStreamPacketization::H264` from `avcC`; H.265 config adds `VideoStreamPacketization::H265` from parseable `hvcC` and rejects missing/invalid `hvcC` as typed `UnsupportedVideoCodec` when packetization cannot be proven from `TrackInfo`. The unprobed/no-capability fallback contract (`fallback_frame_contract_for_unprobed_requirement`) must derive P010 for HDR/10-bit streams, not only from explicit `bit_depth`: VP9/HEVC often omit `bit_depth` in the container, so it picks P010 when `bit_depth == Ten` or (`bit_depth` unknown and `requirement.requires_hdr_processing()`, i.e. PQ/HLG color), excluding non-4:2:0 chroma; otherwise NV12. Without this, deferred HDR selection locked onto NV12 and the decoder failed on the real P010 export. `refine_active_video_requirement` reconfigures the decoder stream (not just pipeline state) when the refined output contract changes after the packet probe (e.g. NV12 -> P010), before the first packet reaches the decoder; same-contract refinement does not reconfigure. Focused tests live in `src/session/tests/capability_selection.rs`. Stream-aware `auto` backend selection (2026-06-18): capability_selection is policy-neutral but detects «активный backend не тянет стрим, а другой playable backend может» через `video_backend_reselection_candidate` (сравнивает `playable_video_outputs` по active backend id и `satisfies`). На каждом выборе active video-трека session эмитит `PlayerEvent::VideoBackendSelectionRequested(VideoBackendSelectionRequest { requirement, decodable_by_active_backend })` (re-export из player-core). Если активный backend не справляется, но кандидат есть — вместо fatal session ставит `pending_video_backend_reselection { requirement, track_id }`, media/демакс/аудио остаются живыми (видео отложено), и `select_default_video_track` возвращает `DefaultVideoTrackOutcome::{Selected,NoVideoTrack,BackendReselectionRequested}`. `set_video_backend` после установки нового backend-а вызывает `retry_pending_video_backend_reselection` (активирует отложенный трек на новом backend-е без переоткрытия media) вместо `configure_active_video_decoder_stream`. `refine_active_video_requirement` после probe тоже маршрутизирует mismatch в reselection, если кандидат есть. Worker имеет `RejectPendingVideoBackend { reason }` (метод `PlayerWorker::reject_pending_video_backend`) → `reject_pending_video_backend` фейлит отложенное видео typed `UnsupportedVideoCodec` — это сохраняет «hardware/software не падает в чужой класс backend-а». pending сбрасывается в `reset_media_state`. Политику auto/hardware/software и реальный свап backend-а владеет `app-egui` (см. `mem:render-video/core`).
- Video decoder stream lifecycle is a neutral `video-core` boundary; see `mem:video-core/decoder-stream-boundary`. `player-core` builds config from `TrackInfo`/active requirement and existing `codec_private`, including the expected `VideoFrameContract`, sends it on media open/default track selection, explicit track select, media switch clear, and backend install/restart, but does not hide seek flush/generation advance/pending packet clear inside configure. `PlaybackPipeline` stores the expected active frame contract derived from the selected requirement; `session/tick/video_decoder_io.rs` validates each current-generation decoded frame against that contract before queueing it, releases the frame handle and reports a typed runtime error on mismatch. This check must remain renderer/backend-neutral: `player-core` does not inspect host-planar bytes, import DMA-BUFs, know VAAPI/WGPU, or decide renderer compatibility.
- Session seek transaction boundary lives in `src/session/seek_transaction.rs`: it owns `PlayerSession` seek/scrub command orchestration, active seek diagnostics, trace markers, commit gates, timeout/fail/finish paths, EOF fallback video position, final seek commit position policy, and the player-side landing-frame guard. `SeekCommitState` carries the original public `SeekMode` so runtime policy stays explicit after container-level mapping: `SeekMode::Accurate` with video may ask the demuxer for a decode-safe point before target, but `actual_position` is only a decoder anchor; playback clocks, audio trimming/gate, video landing-frame guard, resume video budget, and final commit position use the user target, while decoded video frames before target and complete demuxed audio packets ending before target are seek preroll and must not enter normal playback. Active accurate seek uses fast-preroll intent methods owned by `PlayerSession`: demux/tick may temporarily use the existing audio-catchup video packet limit, decoder I/O may bypass only audio-clock decode-ahead for current-generation Accurate seek video packets until the first target-or-after landing frame, because decoder DPB/reorder output may require packets after the user target. This bypass must not bypass texture capacity, packet-channel backpressure, generation/track checks, codec bootstrap, or requirement refinement; texture-capacity bypass remains restricted to matching-generation pre-target packets when the decoder-side output floor applies. Decoded pre-target frames are dropped/released instead of presented, and dropped complete audio preroll does not consume demux packet budget only while a catch-up deadline bounds the scan. If no catch-up deadline is present, dropped audio preroll still consumes normal demux budget so manual/test zero-deadline runs remain bounded. `SeekMode::KeyframeBefore` keeps `actual_position` as the runtime anchor and may commit the actually presented current-generation frame before target. EOF fallback and no-video/audio-only commits stay separate from this guard. `SeekRuntimeState` in `src/seek_state.rs` holds session-owned seek runtime fields (`commit`, trace, simple scrub, EOF fallback marker), while decoder flush, generation advance, pending packet clear, decoder state reset, clock reset, and demux seek remain explicit lifecycle steps at the session boundary.
- Accurate seek decoder output-floor integration (RC3 rollback): `seek_transaction` owns Accurate floor lifecycle and stores only session-level marker `{ generation, floor_pts }` when decoder returns `Applied`/`Unchanged`; `PlaybackPipeline` exposes neutral `set_video_decoder_preroll_output_floor` / `clear_video_decoder_preroll_output_floor` wrappers that preserve `AbsentDecoder`, `Unsupported`, `Backpressure`, and `Fatal` result states. Accurate seek with selected video sets floor after accepted demux seek using the user target PTS and current generation, with `retain_latest_before_floor = true`; `Unsupported`/`AbsentDecoder`/`Backpressure` keep the old player-side pre-target decoded-frame drop/fallback path, while `Fatal` becomes a runtime decoder error instead of silently continuing. Floor is explicitly cleared on final commit, timeout, replacement seek, media reset, and backend replacement. `session/tick/video_decoder_io.rs` may bypass texture-capacity backpressure only for matching-generation pre-target Accurate preroll packets with decoder-side floor applied; packet channel backpressure, stale generation, track validation, codec bootstrap/refinement, and in-flight accounting remain unchanged. `VideoDecoderDiagnosticEvent::SeekPrerollFrameSuppressed` increments existing Accurate `decoded_pre_target_frames_dropped` only for active matching generation. The old RC3 landing-preview scheduler branch is removed: during active Accurate gate-wait with an empty present queue the scheduler repeats the current pre-seek present frame until a target-or-after frame arrives; near-EOF fallback presenter/commit path remains separate and still waits for decoder drain.
- `PlayerSession` unit tests are split out of `crates/player-core/src/session.rs` into `crates/player-core/src/session/tests/`: `mod.rs` holds shared imports/module wiring, `test_support.rs` holds shared fakes/builders, and domain files cover audio runtime, media lifecycle, seek regressions/transactions, decoder boundary, scrub, playback, EOF drain, capability selection, and seek trace behavior.
- `session/tick/demux_admission.rs` owns demux/audio priority policy and distinguishes video capacity from selected-audio bootstrap/catch-up/read-ahead: selected audio without output may demux until the first pending audio packet, selected audio with output above high-water blocks demux even when the pending audio queue is empty, low-water catch-up can still read through bounded video packet pressure, and video-only present queue backpressure remains unchanged.
- `session/tick/presentation_scheduler.rs` owns present/drop/repeat, scheduler timing diagnostics, present queue/decode-ahead limit helpers, and bounded adaptive catch-up; it must preserve 24/30/59.94 frame timing, late-drop grace, seek preroll fallback, and video-only present queue pressure.
- `session/tick/wakeup.rs` owns `PlayerWorkerWakeupPlan` and read-only worker wakeup planning. `PlayerWorkerWakeupPlan` includes `wait_for_decoder_activity` as an intent flag for worker-side event waits. Decoder readiness must not schedule `Duration::ZERO` merely to poll decoder progress during active Accurate preroll; it uses a bounded non-zero `DecodeReadiness` fallback (or an earlier audio/frame deadline) and sets the activity-wait flag only when the pre-snapshotted neutral decoder activity status is available. `PlayerSession::worker_wakeup_plan_with_decoder_activity_status` is the internal boundary that lets `PlayerWorkerRuntime` snapshot decoder activity before planning, then attach/use that snapshot only if the final plan still requests `wait_for_decoder_activity`. Worker timed waits keep command/shutdown priority with `select_biased!`, check `activity_since(last_observed_epoch)` before entering select to close the lost-wakeup window, update the observed epoch only on `ActivityReceived`, drain stale `NoNewActivityAfterEpoch` pulses without moving the original absolute playback deadline, and disable the current activity source after fatal/disconnected notifier so the same ready-disconnected receiver is not selected in a tight loop. Legitimate zero wakeups remain reserved for front-frame-ready, real pending audio/video/demux work, and state-machine work that can progress immediately. EOF coarse progress and worker wakeup fairness must stay intact.
- Session decoder I/O scheduler lives in `src/session/tick/video_decoder_io.rs`: it owns stateless decoder-boundary orchestration for draining decoder diagnostics/errors, completed packet acks, decoded frame intake, pending video packet send, bootstrap/keyframe gating, stale-generation drops, and packet-level requirement refinement. Software host-upload send backpressure is integrated here through neutral `PlaybackPipeline::video_decoder_send_backpressure()`/`HostUploadResourceSnapshotStatus`: packet send, demux admission, catch-up, and wakeup planning pause on ready host frame queue full, upload slots exhausted, or software decoder control-channel full; `UnsupportedBackend` keeps the old VAAPI `decoder_resource_snapshot()` surface accounting path. Callers pass typed `VideoDecoderIoLimits`/`VideoDecoderIoContext` built from tick config; seek lifecycle remains session-owned and decoder I/O must use session boundary methods for seek trace/gates and existing frame/texture release helpers. H.264 and H.265 bootstrap after seek/flush require codec-aware decode-start proof: `PacketKeyframe::Unknown` stays in dropped-until-keyframe/preroll state and must not be converted into a decoder keyframe hint unless the active codec policy has explicitly allowed that packet. H.264/H.265 packet-level requirement refinement must pass track `codec_private` through the `PlayerSession::video_codec_private_for_track` boundary into `codec_core::probe_video_packet_requirement_with_codec_private`; MP4/MKV length-prefixed packets must not be probed as raw bytes without `avcC`/`hvcC`, because length prefixes can look like AnnexB start codes and cause false profile/packetization failures. Focused regression tests include H.264 codec-private refinement tests plus `h264_unknown_keyframe_bootstrap_packet_waits_for_codec_proof`, `h265_unknown_keyframe_bootstrap_packet_waits_for_codec_proof`, and `unknown_keyframe_bootstrap_packet_is_sent_as_decoder_decode_start`.
- Session EOF drain boundary lives in `src/session/eof_drain.rs`: `PlayerSession` owns private `EofDrainRuntime` and exposes intent methods (`is_eof_draining`, `eof_drain_needs_progress`, `enter_eof_drain`, `start_eof_audio_tail_if_needed`, `finish_eof_drain_if_ready`). Entering EOF drain does not immediately flush or drain the decoder if player-side video tail is still queued: `PlayerSession` first sends already-demuxed pending video packets, waits for decoder in-flight packet ACKs, then starts decoder EOF/DPB drain through the video-core boundary without seek flush, generation advance, or new demux reads. Decoder tail frames continue through normal decoded-frame intake, and EOF completion remains blocked until the decoder reports `Drained` for the current seek generation. It transitions `Draining -> Ended`, keeps the demuxer open for seek/replay from `Draining` and `Ended`, and uses replay seek-to-zero with `PlaybackResumeIntent::Play` for direct Play from EOF.
- `TogglePlayback` treats active states (`Playing`, `Buffering`, `Seeking`, `Draining`) as pause requests; this prevents audible EOF audio tail in `Draining` from becoming replay, while `Ended` still toggles to replay seek-to-zero.
- `PlaybackPipeline::audio_eof_drain_state()` is the boundary for EOF audio tail policy. It distinguishes no selected audio, pending audio packets, absent output, output draining, drained output, and whether output play was already requested.
- During `Draining`, `session/tick/mod.rs` continues processing pending audio packets, sends already-queued video packets to the decoder, drains decoded/video-present queues, and must not read new demux packets after EOF. Active seek EOF fallback remains owned by seek commit logic.
- EOF drain depends on worker wakeup fairness: render feedback events must not slide an already-planned playback deadline forward. `PlayerWorkerRuntime::wait_for_worker_wakeup_with_timeout` preserves the absolute `PlannedWorkerWakeup` deadline while draining render feedback, otherwise continuous redraw can starve coarse EOF/audio-tail ticks.
- Audio-only playback must not rely on the coarse 250 ms worker fallback while the output buffer is above high-water. `session/tick/wakeup.rs` schedules an audio refill wakeup for the moment `audio_buffer_level_ms` reaches the high-water mark, so CPAL does not drain into underrun before demux/decode can refill the ring buffer.
- EOF `Draining` with pending audio packets is still audio refill work even though demuxing is inactive. `session/tick/wakeup.rs` must keep scheduling the refill deadline from current output buffer level for the queued audio tail, while preserving the invariant that `PlayerSession::tick` does not read new demux packets after EOF.
- Video-only EOF drain must keep using the no-audio monotonic media clock. `presentation_clock_position_at` lets `Draining` without active seek use the monotonic fallback, so queued tail video frames become presentable and the session can reach `Ended`.
- EOF completion has a defensive stale-output fallback: after player-owned queues are empty, a positive audio output buffer can be treated as drained only if the audio clock has stopped moving for `audio_stall_timeout`. This is not the confirmed root cause of the 2026 EOF/replay bug; the confirmed audio root cause was worker wakeup starvation.

- Capability selection must include selected track display orientation when building `VideoStreamDecodeConfig`. `player-core` only transports `VideoDisplayOrientation` from `TrackInfo.video.orientation` into the neutral decoder config and diagnostics; it must not apply renderer-specific rotation or inspect container matrices directly.
- Session E hardware Accurate seek validation on Intel i965 confirmed the current seek policy resumes and commits after all parsed manual seeks on H.265/VP9/H.264 stress assets, with target-or-after presentation and no never-resume case. Residual latency risk remains outside the player-side commit guard: heavy/random seeks can still stall on decoder output for roughly 0.5-2.0 s, so future seek-policy work should distinguish decoder-output latency from reclaim/backpressure failures before changing commit or fallback semantics. Session F narrowed one player-side latency source: active Accurate seek now keeps feeding the target-or-after DPB/reorder tail without audio-clock decode-ahead pacing until the first landing frame, and worker WARN diagnostics include total/pre-target/target-or-after seek video packet counters to prove whether delay is feed starvation or decoder throughput/long-GOP distance.


## Session 08B runtime reconfigure boundary (2026-07-11)

- `PlayerWorker::set_video_backend` — request/reply commit boundary с `PlayerVideoBackendInstallIntent`; settings intent немедленно возвращает typed busy при active seek/scrub/pipeline lifecycle.
- Session backend swap временно устанавливает staged decoder handle и восстанавливает прежний handle/backend id при configure/re-seek failure; двойная apply+rollback ошибка остаётся отдельной `ApplyAndRollbackFailed`.
- Accepted decoder-thread config фиксируется в worker config только после успешного backend commit.
- Audio output device live apply сначала создаёт/запускает replacement output, затем атомарно заменяет active output/clock; failure оставляет прежний output рабочим.
- `PlayerRuntimeSettingsUpdate::requires_pipeline_lifecycle_boundary` обеспечивает preflight до mutation смешанного request-а, поэтому busy не успевает изменить default-volume/tick owners.


- Session 09 implements both real public scrub release policies with player-owned visible-frame stable identity/timing, strict stale guards, exact latest-target fallback, and typed command outcomes. See `mem:player-core/scrub-commit-policy-s09`.


## Dormant scrub-driver cleanup (2026-07-11, maintenance Session 12)

- `session/scrub_driver.rs` не имеет module-wide `dead_code` suppression. Production driver сохраняет только реально вызываемый `submit_target_update -> drive_step` path; безссылочный `cancel_active` route и lifecycle result/error variants, которые production implementation не мог построить, удалены без подключения нового lifecycle behavior.
- `AudioResumeTimingInput::known` и `PlaybackPipeline::can_send_video_decode_packets` являются focused test helpers и компилируются только под `cfg(test)`. Media load по-прежнему всегда сбрасывает playback rate в `1.0x`; future-only `PreserveAcrossPlaylist` и одно-вариантная policy abstraction удалены.


## Session 18 seek decomposition (2026-07-11)

- `session/seek_diagnostics.rs` owns active-seek observation, trace counters and diagnostics snapshot assembly; mutable seek state remains in `SeekRuntimeState` and lifecycle remains in `PlayerSession`.
- `session/seek_commit_gates.rs` owns read-only commit-gate evaluation, typed soft-audio fallback decision, target/generation matching, EOF fallback readiness and progress-blocker classification. Public API and seek semantics are unchanged.
- Focused characterization tests are split into `session/tests/seek_diagnostics.rs` and `session/tests/seek_commit_gates.rs`; transaction/start/scrub/cleanup tests remain in their existing responsibility modules.
