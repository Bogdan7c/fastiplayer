# S28G existing-demux hardening gate (2026-07-22)

- S28G сводит S28A/B/C без новой runtime feature logic и без изменения public/internal runtime API. Aggregate evidence: `docs/existing-demux-s28g.md`.
- Ownership остаётся прежним: `demux-api` владеет neutral inputs, bounded sniff/replay, exact registration capabilities и `CompositeAvDemuxer`; `symphonia-demux` владеет adapter/events/seek/finite ordered wrapper/signature mapping; exact `symphonia-format-isomp4`, `symphonia-format-mkv` и `symphonia-format-caf` patches владеют container packet parsing.
- Matroska registration теперь имеет отдельное hermetic evidence с exact `DocType=matroska`: `factory::tests::matroska::exact_matroska_doctype_opens_local_and_ordered_inputs` проверяет signature detection без hint-а, exact seekable/streaming/ordered capabilities, local open и finite ordered open. Generated WebM больше не считается доказательством Matroska row.
- Factory descriptor получил truthful fMP4 identity `symphonia/generated-fmp4-s28a`. `descriptor_lists_exact_s28_foundation_fixture_ids` закрепляет полный S28A/B/C fixture set, включая `symphonia/generated-matroska-ordered-s28b` и восемь S28C identities.
- `symphonia-demux::matroska_metadata` остаётся bounded fail-open structural indexer только для `Tracks`/`Colour` и `SeekHead`/`Cues`. Exact MKV patch остаётся единственным owner-ом `Cluster`/`Block`/lacing/packet payload parsing. Test `cluster_payload_is_opaque_to_bounded_metadata_and_cue_indexer` доказывает, что ложные Tracks/Cues внутри Cluster не видны indexer-у.
- `scripts/check-refactor-guardrails.py` запрещает alternative existing-container parser dependencies у `symphonia-demux`, проверяет только production Matroska packet-parser declarations (не слова в комментариях/assertions/test corpus), требует exact evidence anchors и сохраняет `demux-api`, `symphonia-demux`, `web-media-http` в blocking coverage inventory.
- `coverage/baseline.json` и `coverage/exceptions.json` не менялись. `scripts/coverage.sh check` остаётся NOT PASS из-за известного Session 28 inline-vs-external test relocation/ratchet blocker-а; это отдельный policy package.
- Verification: Rust 1.96 focused suite для `demux-api`, `symphonia-demux`, `web-media-http`, `web-media-playback-plan`, `codec-core`, `audio`; direct locked isomp4/mkv/caf patch tests; dependency-patch integration; format/refactor guardrails; strict `symphonia-demux` Clippy; fmt/diff checks. Dependencies blocking checks прошли, non-blocking advisory report отдельно показывает external unmaintained `audiopus_sys` и `ttf-parser`.

Связанные memories: `mem:symphonia-demux/core`, `mem:symphonia-demux/iso-bmff-fmp4-s28a-2026-07-22`, `mem:symphonia-demux/matroska-webm-s28b-2026-07-22`, `mem:symphonia-demux/audio-containers-s28c-2026-07-22`, `mem:demux-api/core`, `mem:testing/coverage`.