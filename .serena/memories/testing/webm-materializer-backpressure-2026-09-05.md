# Offscreen WebM consumer respects descriptor backpressure

Final rename repeatability at fcb0097e caught a real test contract defect: third measured app run failed `n14b_lifecycle_http_webm_close_restart_reaches_submitted_readback_without_extractor` in `direct_progressive_webm.rs:145`, which treated the first `HostPlanarWgpuTextureViewLookup::Busy` as impossible. The production materializer intentionally forwards a Busy from `PresentFrameResourceProvider::try_resource_descriptor_lookup`; the asynchronous FFmpeg decoder can still hold the resource-pool mutex just after frame publication. This is not a fatal playback result and not evidence that a preceding GPU fence was lost.

Fix is test-only: the offscreen consumer calls test-only extension method `wait_host_planar_texture_view_ready`, bounded to ten seconds and yielding CPU only on Busy. Other typed outcomes pass through unchanged; the existing harness still proves WGPU draw/nonblack readback/submitted-resource release. Previous submit completion is already handled separately by the harness. Production APIs, scheduler, render/decoder ownership, baseline, policy and exception ledger do not change.

New excluded helper/test module: `crates/app-egui/src/web_media_open/content_probe_tests/tests/materialization_backpressure.rs`, included as a child of direct_progressive_webm. A one-shot Busy provider wraps the real FFmpeg provider and forces Busy before actual decoded VP9 rendering and release. The regression fails with the old immediate lookup and passes with the bounded consumer wait. The call-site retains its receiver-method form and the original 35-character method-name width, preserving both the receiver and full-expression LLVM spans. A free-function replacement preserved only one of these spans and was rejected by the diagnostic coverage comparison. No padding or source exclusions were added.

Focused command: `cargo test -p app-egui --all-features --locked direct_progressive_webm`. Final qualification outcomes remain in `mem:global/fastiplayer-rename-verification-2026-09-05`; failed/cancelled attempts are not passes. The successful fcb0097e first fresh check and its automatic CI are historical evidence after this test fix; two final-source fresh checks and final-SHA automatic CI are still required.