# S42 core/services production-boundary split — executor wave 3 (2026-08-27)

- Behavior-neutral private production splits preserve all external and crate-visible API paths through parent re-exports; no manifests, baselines or tests were moved.
- `crates/source-core/src/http_bounded.rs` is 594 lines and owns request construction plus shared status/redirect/Range validation and buffered frontends. `crates/source-core/src/http_bounded/streaming_body.rs` is 276 lines and owns `HttpBoundedStreamingBody`, `HttpBoundedStreamingFetchHop`, response lifetime, per-poll timeout/cancellation, exact EOF/body-limit accounting and `HttpSourceSession::open_bounded_single_hop_stream`.
- `crates/web-media-dash/src/live.rs` is 332 lines and owns selected snapshot construction plus publish ordering/atomic replacement. `crates/web-media-dash/src/live/availability.rs` is 509 lines and owns synchronized availability, checked ATO, A/V lane intersection, live edge/window math and selected Period/Representation continuity.
- `crates/web-media-playback-plan/src/planner.rs` is 525 lines and owns candidate evaluation and deterministic playable ranking. `crates/web-media-playback-plan/src/planner/rejections.rs` is 316 lines and owns typed rejection identities/reasons, ordered evaluation projection, bounded secret-safe aggregation and planning error display.
- Preserved invariants: HTTP cancel/timeout/range/body bounds and redacted locator diagnostics; DASH live edge plus A/V intersection and Period continuity; planner snapshot/reason order, exact+semantic identities and identity-free safe summaries.
- Verification PASS on Rust 1.96.0: individual all-features locked package suites (`source-core` 82, `web-media-dash` 48 across unit/integration, `web-media-playback-plan` 30), combined affected-package all-features locked tests, strict all-targets Clippy with `-D warnings`, `cargo fmt --all -- --check`, `git diff --check`, refactor guardrails and Serena diagnostics for all six files.
- `python3 scripts/check_s42_guardrails.py` remains status 1 only for the coordinated pre-existing oversized/stale-baseline inventory; none of the six split files appears in its violations. `scripts/module-size-baseline.json` was intentionally not edited.

Related: `mem:testing/s42-core-services-test-layout-2026-08-27`, `mem:media-services/progressive-web-hardening-s27-2026-07-22`, `mem:media-services/dash-live-s35-2026-07-24`, `mem:media-services/web-playback-planner-s21c-2026-07-21`.
