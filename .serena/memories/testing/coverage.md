# Stable coverage v2 (authoritative, updated 2026-09-05)

## Owner-approved S08 workflow change (2026-09-05)

Owner explicitly approved moving the expensive three-run coverage measurement out of required push/PR CI into `.github/workflows/coverage.yml` (`Coverage (manual)`, workflow_dispatch only). `scripts/coverage.sh check` still performs the same full build, three runs, stable-coordinate ratchet and artifact validation; no baseline or measurement exception was lowered. CI keeps a fast `Coverage baseline policy` job: tracked schema validation on push/PR and exact previous/proposed pair validation on PR. All functional workspace, no-default-feature, WGPU/FFmpeg vertical seek, Clippy and other checks remain automatic. Historical references below to blocking measurement describe the manual command's exit semantics, not an obligatory main/PR coverage status.

Reason: the old coverage CI spent18m15s on clean instrumented build plus three full runs. The50k shared-payload oracle itself was O(N²); S08 replaced repeated queue/snapshot searches with length-checked pairwise IDs and Arc sharing assertions over all50000rows. Ordinary all-features playlist-core130tests now0.65s versus183.10s in the earlier local run. Production snapshot logic unchanged. New split-workflow validator is `scripts/tests/coverage_split_workflow_contract.py`; existing adversarial shell/env tests retained via a validated view of both actual workflows.

The final automatic old coverage run33931378033 at source1cc181e1 passed all three test executions but failed stable-coordinate ratchet. This failure is retained as historical evidence and is NOT described as fixed by the workflow move. No automatic rebaseline/retry is authorized by the move.


Этот документ полностью заменяет прежние Session 07B/28/S42 утверждения об aggregate v1 baseline и одном measured run. Историческая модель сохранена только внутри `coverage/baseline.json.legacy_report_only` как provenance и никогда не участвует в blocking decision.

## Владельцы и tracked policy

- `scripts/coverage.sh` — единственный shell entrypoint: `check` проверяет tracked v2 baseline, `bootstrap <proposal>` создаёт review-only proposal и не меняет tracked policy.
- `scripts/coverage_stability.py` + `scripts/coverage_stability_schema.py` владеют source-coordinate run/cohort/baseline schemas, stable ratchet, atomic baseline-update policy и measurement-exception lifecycle.
- `coverage/baseline.json` — schema v2. Текущий S03 tracked baseline квалифицирован exact 9/9 intersection трёх независимых cohort-ов после S01/S02 module split и S03 stabilization; raw SHA-256 `4f50fbc2b48e5e4858d8a377f41af8d54d1d14b4184130c95e1bc32c8e30b2da`, logical `sha256:ed3320f87c1c353f89aeeb181fa2cbf20f057dbe83e95032e5080119d700773a`, source-files hash `sha256:175a8a56855dd50d5f07bde052d5ffe6dfb0e429fc381f7fd40bbcb3bcac699f`, 992 source-файла. Финальные cohort hashes: `sha256:435ae105a59985a757f9c225fd0008fbfd68fe49498fa978e550f468ab1180b3`, `sha256:0443fbe772dbc357f6bc75b67a1fd5070be5d5432b550b1591d10c04cb6e4124`, `sha256:54928c449752f63b64a3c0d3ca0fc9dedf7ce50bb5c836d5c3193950fb6b5f9b`. Два fresh `scripts/coverage.sh check` прошли с пустыми `regressions`/`universe_changes`; cohort hashes `sha256:b7b956951e8f8b3b2b8f4007f5d90d81240af677ad4a9665821aeaa9c4dc0aa5` и `sha256:121c309ed45d3996dd8d5e3d6b9c0d2de49462da2baa981507704b7465d121fb`, общий check hash `sha256:7785ad71cb726dee9d314ed212385c589b09a52d442c6bcd43a071dacc99cc2e`.
- `coverage/measurement-exceptions.json` — единственный blocking exception ledger, schema v1; S03 ledger raw SHA-256 `6ad36935e47b817ad99b7bb770795e1970487081fe4df0f6b9d10f6e1c73b7c9`. Он содержит ровно пять bounded cross-universe transition rows: `blocking-group/functions` 9868/11701→9917/11760, `crate:web-media-adaptive/functions` 197/227→208/240, `crate:web-media-dash/functions` 377/460→402/491, `crate:web-media-smooth/functions` 201/246→204/251 и `crate:web-media-smooth/regions` 3166/3656→3190/3684; review_by 2026-12-04. Same-universe loss не разрешает.
- `coverage/exceptions.json` и embedded v1 baseline — frozen `legacy_report_only` provenance. Они не разрешают v2 regression и не являются параллельным источником истины.
- `coverage/policy.json` по-прежнему классифицирует blocking/informational crates. `coverage/executable-inventory-policy.json` типизированно разрешает runtime-built root только для `settings-derive/tests/trybuild`.

## Методология

1. Один `cargo test --no-run` materializes parent executable cohort.
2. Exact stale runtime root transactionally quarantined. Typed cargo-test prewarm materializes trybuild; prewarm profiles удаляются.
3. Source tree, parent executable inventory и runtime trybuild inventory freeze-ятся до measured run 1. Build/runtime semantic identity — logical path + mode + size + SHA-256; symlink/root escape/add/remove/mode/content change fail closed. Parent metadata optimization может пропускать повторный hash только после runtime ctime probe; mtime/inode сами по себе не semantic.
4. Ровно три measured workspace runs выполняются с обычной concurrency. Source coordinate классифицируется: 3/3 — stable; 1/3 или 2/3 — variable diagnostic; 0/3 — uncovered. Blocking ratchet использует stable coordinates, а variable remains visible report-only.
5. Run/cohort/artifact publication transactional. Build, runner или publication failure восстанавливает предыдущие merge metadata и quarantined runtime root; partial stage/prewarm/run1/run2 profiles не публикуются. После успешной публикации полного cohort semantic ratchet выполняется отдельно: его exit 1 намеренно сохраняет новый cohort, variable diagnostics и `check.json` как evidence регрессии, но не меняет tracked baseline/exception ledger и не считается успешным gate. Successful cohort retains exact authoritative run3 profraw set + profdata/list for report artifact.
6. Runtime builder policy, source identity, toolchain identity, build/runtime inventories and every published artifact are recorded in schema-v2 cohort manifest. New runtime owner fails until explicitly modeled.

Текущий independently qualified S03 baseline принят на Rust 1.96.0, LLVM 22.1.2 и cargo-llvm-cov 0.8.7; source-files hash `sha256:175a8a56855dd50d5f07bde052d5ffe6dfb0e429fc381f7fd40bbcb3bcac699f`. Exact workspace intersection: functions 15,800/20,033, lines 164,686/212,402, regions 206,815/270,182. Blocking intersection: functions 9,917/11,760, lines 100,352/116,605, regions 125,944/149,522. S03 file-local audit охватил 76 changed Rust files от base `d61a2d87082e4e57de6c550691049bce50e0449a`; parent/new-module deltas и точечные stabilization changes проверены до установки tracked pair. Полный S03 audit: `mem:public-launch/s03-coverage-qualification-2026-09-04`.

Для baseline update после конкурентных test-only правок один cohort статистически недостаточен. Обязательный human-reviewed qualification workflow: три независимых cohort-а на одной source revision (9 measured workspace runs), exact 9-run stable intersection, file-local audit каждого изменённого файла и два свежих обычных repeatability-check после установки tracked baseline. Aggregate workspace ratio не может скрывать file-local stable loss. CLI пока не автоматизирует cross-cohort reducer; evidence хранится вне tracked policy и сверяется вручную.

Внутри каждого cohort executable logical paths/mode/size/SHA-256 обязаны быть byte-identical. Между независимыми cohort-ами ELF SHA может различаться из-за linker/compiler nondeterminism только если source/tool/profile и coordinate universes совпадают, а каждый cohort отдельно проходит fail-closed manifest validation. Это осознанная граница измерения, не гарантия reproducible build. Реальный test/build failure остаётся failure: retry, measurement exception или ослабление baseline его не легализуют.

G2 qualification поймала две реальные scheduler-sensitive fixture race до установки baseline: `playlist-state::resume::worker` полагался на случайный disconnect wake sender, а app dynamic-options shutdown не синхронизировал active+retired admission и idle fallthrough. Regression commits `169ade5c` и `3f9d5f90` добавили explicit join/started-call/idle lifecycle oracles; production semantics не менялись. S03 дополнительно выявила schedule-dependent Vorbis request count, immediate-worker polling branches, HLS availability read до packet evidence и `wait_cancelled()` вызов после pre-cancel race. Все исправлены причинными functional handshakes/bounded assertions. Один отвергнутый `cfg(test)` hook менял CodeRegion map одной production function между unit/dependency binaries и был заменён одинаково компилируемым private observer implementation. Qualification после каждой Rust-правки начиналась заново; неуспешные/stale cohorts и proposals не смешивались с финальными evidence.

## Baseline update policy

PR comparison is one atomic pair:
`python3 scripts/coverage_stability.py check-baseline-update --previous-baseline ... --previous-measurement-exceptions ... --proposed-baseline ... --proposed-measurement-exceptions ...`.

All four inputs are required; command is read-only. Exit 0 = allowed, 1 = well-formed semantic policy violation, 2 = malformed/schema/redaction/I/O failure. Same-universe stable loss cannot be excepted. Cross-universe changes require exact proposed bounded rows; previous rows do not authorize a new decrease. Unknown, stale, overbroad and malformed rows fail closed. Recovery from an expired previous ledger is allowed only by a clean proposed pair, so policy cannot deadlock.

Legacy `coverage_metrics.py check-baseline-update` was deleted; v1 report-only helpers remain for provenance validation only.

## Commands and tests

- Full local/CI gate: `scripts/coverage.sh check`.
- Review-only proposal: `scripts/coverage.sh bootstrap target/coverage/<proposal>.json`.
- Baseline/ledger validation: `python3 scripts/coverage_stability.py validate --kind baseline|measurement-exceptions --input <path>`.
- Focused tests: `scripts/tests/test_coverage_baseline_update.py`, `test_coverage_stability.py`, `test_coverage_metrics.py`, runner/inventory/quarantine/publication suites.
- CI wiring oracle: `scripts/tests/test_s42_release_runner.py::S42ReleaseRunnerTests::test_coverage_check_composes_stable_preflight_suite_and_ratchet`; pure canonical workflow parser: `scripts/tests/coverage_workflow_contract.py`.
- Human contract: `docs/code-coverage.md`; CI overview: `docs/continuous-integration.md`.


## S12 diagnostic finding (2026-09-05; fix pending)

LLVM 22.1.2 combined multi-object export can erase executed coverage based on object order. Reproduced with immutable aud005_packet_ack_loss-f0d69a04cd253e17 and matroska_decode_requirement-1097bd05d94baaa3 binaries at aa30af3d: identical merged profile, codec-core ChromaSubsampling::from_matroska_subsampling count is 0 with the former object first and 6 with the latter first (6 when exported alone). All measured tests passed. This supersedes constant-folding-only explanation of the S12 codec loss. Owner approved per-executable export and exact coordinate union. Implemented adapter scripts/coverage_object_export.py captures actual pinned-wrapper object argv/source filters, verifies frozen executable hashes and profile immutability, and archives each JSON+hash under objects/run-N/. scripts/coverage_object_union.py validates each report and unions exact coordinate universes/covered sets; duplicate copies never inflate counts, workspace source inventory must match, malformed/missing/modified reports fail closed. Runner keeps 3-run intersection and existing source/profile/build freezes. cohort-manifest.coordinate_export marks corrected implementation of existing OR semantics; legacy raw/summary/LCOV/HTML stay report-only. Functional tests scripts/tests/test_coverage_object_union.py cover exporter→extractor→union, both conflicting orders, duplicate/missing/invalid/changed artifacts and source mismatch. Full saved real workspace diagnostic recovers all missing codec function regions without denominator change. Baseline qualification and final remote CI remain pending; do not label a diagnostic run as qualification or lower baseline without required review. Private global S11/S12 handoff holds full diagnostic paths and pending status; public main remains 38ac21c, alpha unreleased.


## S12 nine-run baseline qualification (2026-09-05)

Per-object export implementation c8bd926b09819db8b040f9d89338db5a2c7a73b2 completed 3 independently built cohorts /9 successful measured workspace runs, identical source inventory 6bc541cae84e8be6cbb32cd82cbd88a4bd84bd24fde70d924fadb1a7cd3f9723 (2344 files), matching tool/profile/universes and immutable binaries within each cohort. Exact9intersection baseline hash sha256:f2a813b52ec90dc995a21737563cf9378b7ccb1514b6e6fd6fb51ea95e45cd49 replaces ed3320f8. File-local review and paired baseline-update validator PASS. Ten exact bounded cross-universe rows explain deleted covered cancellation/playlist test code plus S08 informational hardware/UI additions; no source exclusions or blocking-owner changes. Sole unchanged-file loss is cfg(test) entry.rs:623 no-match branch no longer used after quadratic helper removal. Former loop brace handle.rs:289 is new294 (stable); new295 is added non-executable lock scope, not lost production coverage. Review deadline2026-12-04. Legacy v1 provenance unchanged. Public evidence doc docs/coverage-qualification-2026-09-05.md contains hashes, file audit, exact fractions. Two fresh check passes after installation and complete remote CI on final SHA are still mandatory; runtime results belong in private global handoff so the qualified source stays clean.


S12 first fresh check at 160d34bc failed two exact cancellation coordinates with unchanged universes, despite all Rust suites passing. Added deterministic consumer tests in mpeg-ts-demux/tests/cancellation_priority and web-media-adaptive/tests/blocking_resource_fetch/cancellation_priority; adaptive read-only cfg(test) observer lives under owner module in tests/restartable_read_observation. Baseline f2a813 remains unchanged, coordinates must be retained. Two fresh exact-source checks plus full remote CI remain mandatory; runtime results recorded privately.

S12 subsequent check at31d0d777 retained both cancellation branches but lost metadata_sort Probing read-model branch and pending_probe_contract helper pending-loop lines112/113. New excluded test pending_probe_contract::premature_probe_wakes (file metadata_sort/tests/premature_probe_wakes.rs) sends two rendezvous notifications before releasing actual probe, guaranteeing first pending iteration, then forwards real wakes and verifies canonical sort commit+metadata patch. No baseline reduction; fresh checks required. Source universes must remain unchanged.

S12 at57619480 first fresh check PASS12e6e826, second FAIL609e1583 only video-ffmpeg/decoder_thread/worker.rs38 early shutdown (universes unchanged, alltestsPASS). Tests now in decoder_thread/tests/shutdown_priority.rs preset requested/disconnected shutdown BEFORE true worker starts, assert queuedpacket untouched, no completion/frame/error, publishers disconnected, bounded actual termination. No baseline reduction. NewSHA needs twofreshchecks and fullremoteCI; 57619480 automatic21jobsPASS are historical only.


Owner decision (2026-09-05, supersedes pending hardware-scope discussion): GBM/VA-API tests requiring physical hardware execute only on the local machine, never on remote CI. Compile/header/dependency and hermetic software tests remain remote. Implement explicit hosted execution scope while retaining full hardware coverage diagnostics and local qualification; this does not authorize waiving unrelated software losses or publishing before full final-SHA remote CI. Current public candidate2f5d44a5 has automatic21jobsPASS, manual coverageFAIL despite all suitesPASS;158hardware lines plus16software lines need scoped treatment/root fixes. Runtime details remain in private handoff.

S12 implementation of local hardware decision: scripts/test_execution_scope.py owns explicit RUSTIPLAYER_TEST_SCOPE=local|hosted and five filtered libtest names (two GBM tests, three DMA-heap tests). CI/coverage workflows declare hosted; missing/wrong scope in GitHub fails closed. ci-checks workspace and dependency-integration suites plus coverage runner share the command constructor; all software tests, SDK/header/dependency checks stay remote. Full measurements/baseline remain unchanged. scripts/coverage_execution_scope.py routes only same-universe workspace exact coordinate losses in video-vaapi/{gbm_allocator,linear_gbm_frame,dma_heap}.rs into explicit local_hardware_losses; full local check still blocks these losses. Hosted check rejects mismatched/missing cohort execution-scope manifest. Source/provenance/universe/ratio changes and other losses still fail. Every release requires full local and hosted qualification on same final SHA. Hardware absence is reported unavailable, not executed PASS.

Remaining software losses from remote2f5 are addressed by causal tests: local_file_open/tests/shutdown_observation.rs holds release until worker has sampled pending=false; media_install/tests.rs verifies wait_for_signal missing-owner error through actual receipt; video-ffmpeg/decoder_thread/tests/eof_pool_continuation.rs primes a real VP9 packet with zero receive budget, releases a full real pool, consumes renderer-accessible tail resource and verifies final EOF; app-egui/content_probe_tests/tests/eof_render.rs schedules pending→frame→drained publication of a genuinely decoded VP9 resource through existing EOF consumer, then proves WGPU submit/readback/release. No production Rust API/runtime changes. Focused tests pass; full exact-source local repeatability and remote CI remain pending. No release yet.