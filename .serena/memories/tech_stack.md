# Tech Stack

## S42 release/acceptance tooling (2026-07-25)

- Automated S42 composition is `scripts/final-acceptance.sh`: exact Rust 1.96.0 runs format/guardrails, dependency policy, seven standalone patch suites plus workspace patch integration, strict all-target/all-feature Clippy, rustdoc, workspace tests and app feature-off check; exact Rust 1.92.0 runs the locked MSRV check. Cargo-deny is pinned to 0.20.2, cargo-machete to 0.9.2 and cargo-llvm-cov to 0.8.7. The final run completed `S42 automated acceptance: PASS`.
- Coverage policy contains 47 blocking and 11 informational first-party crates. The owner-approved S42 rebaseline has exactly 28 measured `scope/metric` exceptions through `2026-10-25`; no new exception was added for scheduler stabilization. Conservative per-crate baseline envelope: workspace lines 135834/181804, functions 13197/17245, regions 169757/228313; blocking group lines 83276/99646, functions 8338/10114, regions 103632/125867. Latest final clean artifact was higher at workspace 135842 lines/13200 functions/169766 regions and blocking 83284 lines/8341 functions/103641 regions with unchanged totals.
- `validate-baseline` always validates exceptions schema, counters, deadline and duplicate identities before LLVM work; `check-baseline-update` alone matches old→allowed counters. Raw LCOV top-bit execution-counter corruption is rejected fail-closed before baseline or report publication.
- S42 adds no new external production dependency. HDS executable evidence uses existing first-party `flv-demux` and `rustiplayer-config` only as `web-media-hds` dev dependencies. Seven standalone replacement crates remain the exact dependency-patch inventory. Cargo-deny leaves only explicitly nonblocking RUSTSEC-2026-0150 (`audiopus_sys`) and RUSTSEC-2026-0192 (`ttf-parser`) unmaintained advisories with no safe upgrade; XML advisory graph is clean.
- Hardware scope is unchanged except the owner-approved exact `VAProfileH264Baseline` → H.264 Baseline 8-bit YUV420/NV12 capability-intersection mapping. Current hardware manual rerun and the 29-case URL/fixture matrix are `NOT RUN`; FFmpeg remains software decode-only.

- Progressive FTP(S) uses `suppaftp` 10 (`rustls-ring`) inside `source-core` for passive `TYPE I`/`SIZE`/`REST`+`RETR`; thin provider crate `web-media-ftp` (`progressive-ftp`) has only `source-core` + `web-media-transport-api` (+ `thiserror`). `ftps` is explicit TLS only. S21T targets are `TransportRequestTarget::{Http,Ftp}`. See `mem:media-services/progressive-ftp-s37-2026-07-25`.

- `web-media-smooth` is the guarded concrete Smooth static-VOD preparation, selected fragment-source and neutral demux/seek orchestration owner. It reuses S31 transport, sealed S36D/F2/F1 contracts, C3 values and `demux-api`, but has no service/app/player/concrete-HTTP or concrete-Symphonia dependency. App injects the existing S28A/F3A registry, and `ProgressiveDemuxer` owns blocking readiness plus transactional receipts. See `mem:media-services/smooth-manifest-catalog-s36p2-2026-07-25`, `mem:media-services/smooth-fragment-sources-s36p3-2026-07-25` and `mem:media-services/smooth-vod-runtime-s36p4-p6-2026-07-25`.

- `smooth-streaming-fmp4` is a pure first-party adapter with exactly `smooth-streaming-manifest-core`, `symphonia-format-isomp4`, and `thiserror` normal dependencies. It maps validated Smooth H.264/AAC-LC tracks into generic init/media reconstruction and returns audio overhang only as pending exact PCM clipping; transport/demux/player remain downstream. See `mem:media-services/smooth-streaming-fmp4-mapper-s36f2-2026-07-25`.

- Smooth/PIFF fragmented-MP4 reconstruction reuses the local `symphonia-format-isomp4-patch` as the sole ISO-BMFF owner. Its new opt-in public API separately builds bounded H.264/AAC-LC init segments and canonical media fragments from inspected samples; it does not alter existing demux registrations or local/progressive/HLS/DASH paths and does not own ManifestWindow policy. See `mem:media-services/smooth-streaming-fmp4-s36f1b-2026-07-25`.

- Smooth Streaming VOD manifest parsing uses first-party `smooth-streaming-manifest-core`, a sealed pure S04X-backed MS-SSTR v2.0/v2.2 H.264/AAC-LC contract crate. Its only normal dependencies are `bounded-xml-reader` and `thiserror`; HTTP, fMP4/Symphonia, player, seek and app composition remain separate downstream owners. See `mem:media-services/smooth-streaming-vod-s36d-2026-07-24`.

- Static DASH VOD uses first-party `dash-mpd-core` (pure S04X-backed MPD profile) and `web-media-dash` (bounded exact MPD/serialized transport orchestration). Concrete fMP4/WebM parsing remains in the existing Symphonia registrations injected through `demux-api`; no DASH/container parser dependency was added. Player integration is a provider-neutral receipted seek trait in `player-core`. See `mem:media-services/dash-vod-s34-2026-07-24`.

- Language: Rust, workspace edition `2024`, workspace MSRV `rust-version = "1.92"`, Cargo resolver `2`. Development and CI are pinned to Rust `1.96.0`, while CI validates both Rust `1.92.0` (MSRV) and `1.96.0` separately.
- Main UI/window stack: `winit 0.30`, `egui 0.34`, `egui-winit 0.34`, `egui-wgpu 0.34`.
- Render/GPU: `wgpu 29` with `vulkan` feature; production renderer crates are `render-wgpu-video` (pure WGPU NV12/P010 video renderer/materializer boundary) and `render-wgpu-shell` (WGPU device/surface/egui composition shell).
- Media/container/audio dependencies: upstream `symphonia 0.6` with `all-formats`, `all-codecs`, `all-meta`; `cpal 0.15`; `opus 0.3`; `bytes`; `ringbuf`; `crossbeam-channel`; neutral audio contracts live in local `audio-core`.
- Error/log/config stack: `anyhow`, `thiserror`, `tracing`, `tracing-subscriber`, `serde` derive, `toml 0.9`, `directories 6.0`.
- Optional FFmpeg raw binding: `video-ffmpeg` uses `ffmpeg-sys-next 8.1.0` only behind crate feature `ffmpeg` with `default-features = false` and `avcodec`; `video-ffmpeg` itself has empty defaults, but `app-egui` enables its forwarded `ffmpeg` feature by default, so normal workspace/app builds compile the software FFmpeg path and require FFmpeg dev libs/runtime. Opt out with `cargo build -p app-egui --no-default-features` when needed.
- XML stack: first-party `bounded-xml-reader` owns the only untrusted XML parsing boundary and uses `quick-xml 0.41.0` behind project-owned events and mandatory caller-defined budgets; schema/format semantics remain outside this crate. `playlist-io -> bounded-xml-reader` is the intentional XSPF schema-owner edge; XSPF does not depend on `quick-xml` directly. See `mem:xml/core` and `mem:playlist/io-s06-xspf-2026-07-20`.
- Local patches via root `[replace]`: `cros-libva:0.0.12` -> `crates/cros-libva-patch`; `cros-codecs:0.0.6` -> `crates/cros-codecs-patch`; `symphonia-format-isomp4:0.6.0` -> `crates/symphonia-format-isomp4-patch`; `symphonia-format-caf:0.6.0` -> `crates/symphonia-format-caf-patch`; `symphonia-format-mkv:0.6.0` -> `crates/symphonia-format-mkv-patch`; `symphonia-codec-aac:0.6.0` -> `crates/symphonia-codec-aac-patch`; `wayland-scanner:0.31.10` -> `crates/wayland-scanner-patch`. The Wayland patch is a narrow advisory closure that updates its parser dependency from quick-xml 0.39 to 0.41 without migrating winit/egui/window stack. The CAF and Matroska patches are current media-container compatibility owners; see `mem:symphonia-demux/audio-containers-s28c-2026-07-22`, `mem:symphonia-demux/matroska-webm-s28b-2026-07-22`, and `mem:dependency-patches/core`. These are compatibility patches, not app-owned architecture; keep Serena memories and guardrails honest about which patches are still active.
- Local crates are wired through workspace dependencies; crate package `crates/config` publishes as `rustiplayer-config` / lib `rustiplayer_config`.
- Linux config path is `~/.config/rustiplayer/config.toml`; current config schema version is `7`; unknown TOML fields are rejected and validation after serde is mandatory. Legacy schema v2/v3/v4/v5/v6 are migrated on load where supported; generic web-media settings live under `[yt_dlp]`, while legacy `[youtube]` is accepted only through migration. Schema v7 adds optional `yt_dlp.preferred_video_height` with config-owned validation and default `None`; see `mem:config/schema-v7-quality-preference-2026-07-21`.
- SonarQube is local-only for project key `rustiplayer` at `http://127.0.0.1:9000`; scanner imports Clippy JSON from `target/sonar/clippy-report.json`.
