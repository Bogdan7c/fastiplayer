# Slice G web-media stream picker (2026-07-26)

- `PlaylistRuntime` owns process-lifetime `WebMediaCatalogCoordinator` and session-only `HashMap<PlaylistItemId, WebMediaRememberedPreference>`; preferences never enter config/playlist/resume persistence, are lazily pruned against canonical queue membership, and Detached playback never writes them.
- Catalog discovery is one-worker bounded/latest-only with cooperative source+task cancellation, `AppWakePort`, atomic immutable publication, and exact fencing by Item/Detached scope, parent exact identity, media instance, runtime binding, parent stream generation, and request generation. Deadline shutdown uses `bounded-work-executor::shutdown_and_join_until`; non-cooperative workers are typed detached-deadline outcomes requiring process-owner policy.
- Installed YtDlp sources carry a provider-private `WebMediaCatalogAttachment`; default playback does not wait for sibling proof. App discovery composes playable yt-dlp parents/composed A/V plus HLS/DASH/HDS/Smooth provider-neutral catalogs and publishes only verified choices with safe rejected-sibling accounting. Provider projection enumerates every validated compatible A/V edge, coupled presentation, and selectable video-only/audio-only row; it must not collapse a multi-audio catalog to its first audio or require both axes for standalone rows.
- `web-media-playback-plan` owns source-order-independent grouped opaque ranking. `rank_playable_opaque_alternatives` reuses the canonical `compare_playable`; app choices retain planner-issued parent rank plus provider-owned canonical child rank. `select_grouped_opaque_alternative` preserves the maximum reachable lower facets, then uses rank, and rejects ambiguous duplicate `(preserved facets, rank)` instead of falling back to catalog order.
- URL sidebar production UI is one dependent immediate mode/codec/resolution/FPS/HDR picker. One-option selectors remain visible; missing FPS/HDR project as `Автоматически`; actions expose only parent/catalog generation, facet, and option index. Resolution of a lower facet rechecks the active upper prefix.
- Candidate/provider clicks and automatic remembered restore share the single `PendingSameItemSwitch` strong-install slot. UI actions resolve opaque targets inside the model; automatic restore keeps its opaque target internal and publishes only `AutomaticStreamRestore` pending state. Preference changes only after exact Installed. Missing remembered semantic selection accepts the already Installed provider default once, overwrites memory with the actual fallback, and shows one safe notice. Pending automatic restore is reconciled each frame so a manual switch cannot later be reverted by stale automatic work.
- Guardrails explicitly allow only the approved neutral `web-media-hls -> web-media-core` and `web-media-dash -> web-media-core` catalog edges; concrete demux/service/app reverse dependencies remain rejected.
- Focused coverage includes source-order reversal, ambiguous ranks, dependent-prefix resolution, canonical opaque winner, all compatible provider A/V rows, standalone provider axes, latest-only cancellation/stale suppression, and deadline detach. Final verification: planner 19 tests, app no-default 892 tests, player-core 624 tests, executor 6 tests, strict all-target Clippy, rustfmt, Python guardrails, production guardrails, default app check, locked workspace check, rustdoc, and MSRV workspace check PASS.
