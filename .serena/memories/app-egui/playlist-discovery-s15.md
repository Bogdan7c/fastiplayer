# App target-first sibling discovery (Session 15)

Session 15 completed PASS on 2026-07-15. This memory complements `mem:core`, `mem:playlist/core`, `mem:playlist/discovery`, `mem:app-egui/media-open-coordinator-s10c`, and the handoff in `user/playlist_queue_implementation_plan.md`.

## Target-first ownership and exact install chain
- Explicit local picker and trusted CLI admission both reach the same asynchronous `LocalFileOpenJob`; UI-thread local preparation was removed.
- The job calls Session 10C `prepare_local_open` exactly once and transfers the full `PreparedLocalOpenResult`: already opened `PreparedMedia`, topology classification, duration/full metadata, original native source path, safe label, and fingerprint.
- App builds the target queue draft from the original source path; canonical manifest identity never replaces the locator or source label.
- Exact player staging request is correlated with a controller D08 target-only replacement intent. Only player `ReadyToCommit` triggers fallible queue reservation; then controller begins authorization dispatch, the coordinator returns the authoritative barrier resolution, and exact `Installed` infallibly consumes the token.
- Admission/reservation/revision/allocator/capacity/downstream cancellation failure keeps the old queue, active playback, and allocator watermark and cannot start sibling scan. Sibling discovery starts only after exact target commit exposes the reserved stable target Item ID.
- In-app nonempty replacement still requires the D79 confirmed origin. CLI uses the separate typed trusted startup admission.

## Process-lifetime discovery coordinator
- `playlist_runtime::discovery::PlaylistDiscoveryCoordinator` belongs to `PlaylistRuntime`, not renderer-bound `AppState`; playback advance within the committed queue does not end the scope.
- One controller-owned `SiblingDiscoveryScopeId` type is shared by coordinator, settings transaction port, and future readiness waits; it is distinct from `MediaInstanceId` and `PlaylistItemId`. Active authority also captures discovery job/request/policy revisions and a controller-owned `DiscoveryContinuation`.
- `discovery/manifest_worker.rs` owns one reusable bounded blocking filesystem worker and join authority. `discovery/mapping.rs` owns conversion between app policy/domain records and neutral discovery hints/drafts. `discovery/settings_port.rs` owns the D62 phase intent and exact shared freeze/finalize control. The central coordinator stays below the project module-size threshold.
- `load_siblings=false` completes target-only without manifest/probe I/O. Manifest overflow/failure, executor rejection, cancellation, or later discovery failure preserves target and already accepted batches and publishes a typed target-only/final status.
- Repeat/shuffle policy is converted in app code to ordered neutral `ReprioritizeHint` keys; `playlist-discovery` remains free of playlist policy types.
- D62 production port freezes exact pending-manifest admission or the exact active job, rollback resumes it, and post-persist finalize cancels it. Enabling sibling loading is future-only and changing filter/revision affects only future scopes.
- A record-bearing batch published immediately before freeze may commit while its readiness ACK returns `AdmissionFrozen`; the coordinator retains at most two directional ACK IDs and retries them after exact resume. Failed freeze/resume is a typed settings-stage error rather than an ignored boolean.
- Shutdown closes manifest admission and cancels discovery before persistence flush without waiting for blocking filesystem work; Drop retains join authority.

## Progressive commit invariant
- Only record-bearing `AdmittedBatch` can mutate the queue. `AdmissionAdvanced` and `FrontierReady` are marker-only and never carry or commit records.
- Every batch validates job/request/policy revision, direction, apply semantics, and controller continuation. Natural anchor calculation uses committed manifest-key→stable-ID registry; drafts are ID-less until the domain mutation.
- `playlist-core::PlaylistQueue::insert_discovery_batch` atomically preflights D08 lock, exact structural queue revision, stable anchor membership, 50k capacity, next structural revision, and allocator range. Only then does it allocate stable IDs, splice once, and perform one D14b responsive-shuffle merge preserving the existing upcoming order.
- Controller publishes one structural/dirty revision per accepted batch, advances its continuation to the exact new queue/controller revision, and therefore does not self-cancel. Coordinator binds manifest records only to stable IDs returned by the successful outcome and ACKs only after commit.
- Structural mutations cancel the scope; non-structural actions do not. D43/frontier ownership stays in `playlist-discovery`, so unresolved nearer holes and per-side quota/cap rules are enforced before app sees an admitted batch.
- UI-facing `PlaylistDiscoveryInsertionHint` contains inserted stable IDs and an optional stable before-anchor for future scroll compensation. Session 15 adds no playlist rendering.

## Wake and verification
- Manifest worker and discovery executor use the existing typed AppWake edge. App shell drains on wake and requests redraw only for a visible status/progress/batch change; no mandatory per-job 50-ms polling was added.
- PASS: 71 playlist-core tests, 33 playlist-state tests, 470 app-egui no-default tests, 52 playlist-discovery tests, focused local-open/controller discovery tests, strict touched-crate all-target Clippy, fmt, Rust 1.96 locked workspace check, refactor guardrails, and diff check. Cargo is authoritative; rust-analyzer retained stale diagnostics for the updated local-open enum shape in `state/media_jobs.rs` and intermittently for the new test submodule, while all production discovery modules are otherwise clean.
- Manual Add, visible refresh, metadata Sort preparation, playlist UI rendering, and readiness-driven delayed navigation were outside the original Session 15 scope.

## Session 15A readiness and delayed navigation extension
- Session 15A completed PASS on 2026-07-16. `playlist_runtime::discovery::navigation` is the app-owned marker router and read-only navigation status/action boundary; `AdmittedBatch` membership, ACK, and queue commit orchestration remain unchanged.
- Marker routing validates the active job, request revision, policy revision, direction, and a separate monotonic admission/readiness revision per side. `FrontierReady` resolves its exact manifest key only through the successful committed key→Item-ID registry; duplicate, out-of-order, stale, missing-registry, or wrong-direction events cannot gain action authority.
- Non-shuffle readiness is revalidated by `PlaylistController` against the exact domain target. Responsive shuffle treats `AdmissionAdvanced` only as a wake to query domain-owned committed upcoming; discovery never selects a shuffle target, and admission without a target leaves the same wait/latch armed without polling.
- `PlaylistRuntime` exposes serialized manual navigation, automatic snapshot observation, stop-after-current, one latest typed action slot, and a read-only `Idle | WaitingManual | WaitingAutomatic | TargetReady | Exhausted | Cancelled | Fatal` status model. Traversal priority is updated through the existing neutral job reprioritize boundary; cancelling the transition restores the normal manifest hint and does not cancel or resurrect the bulk scan.
- Every released manual/automatic target remains a `PlannedPlaylistInstall` and enters the existing D08/D39 controller/media-open path. Marker handling itself never mutates current, active identity, factual history, allocator, or dirty state. The D03 skip plan remains an immutable committed-ID snapshot, so late admission is visible only to a new explicit action or later automatic edge.
- PASS: 477 app-egui no-default tests, 77 controller tests, 52 playlist-discovery tests, strict app no-default all-target Clippy, fmt, Rust 1.96 locked workspace check, refactor guardrails, and diff check. Main review repeated the 5 focused controller readiness tests, 2 router revision/direction tests, fmt, guardrails, and diff check. Rust-analyzer may retain stale E0583 only for the two new test submodules even though Cargo and Clippy compile them.
- Manual Add, visible refresh, metadata Sort preparation, and playlist UI rendering remain outside scope. Next allowed playlist session is 16.

## Session 16 Manual Add and visible refresh extension
- Session 16 completed PASS on 2026-07-16. `PlaylistDiscoveryCoordinator` still owns the only `DiscoveryExecutor`; `discovery::action_jobs` owns concurrent Manual Add jobs, one bounded visible-refresh lane, process-lifetime progress/completion state, and named bounds (pending 256, batch 64, valid-fingerprint cache 512). Sidebar visibility does not own lifecycle.
- Every Add cancels the active sibling scope but preserves target/already committed batches. Manual jobs capture a D66 queue generation: Clear, queue replacement, new explicit open, and shutdown cancel/stale them; remove/reorder/sort/another Add do not, so terminal success rebase-appends to the current tail.
- Manual paths are deterministic natural order. Duplicate locators become independent drafts/IDs. Successful records are committed once through controller/domain capped-tail append; explicit cancel, zero success, stale generation, and zero capacity consume no IDs and create no dirty revision. D67 rejected tail is reported separately.
- Visible requests are local-only, deduplicated by Item ID + fingerprint and never scan startup/invisible rows or URL network sources. The discovery worker first reads only the local fingerprint: an exact match completes without demux; a mismatch alone runs the separate metadata probe. Completion revalidates structural revision and applies ID/locator/old-fingerprint guarded metadata+fingerprint patches; stale/removed/replaced rows are rejected and are not marked cache-valid. Failures remain retryable and set only a D70 runtime unavailable badge, never remove/dirty the row.
- The production exact-Installed path updates local and URL last-known metadata from the actual prepared descriptor. D64 Play remains the existing exact single prepared-open revalidation; no discovery probe is inserted before target open. Discovery terminal summaries retain exact failure-category counts independently of bounded diagnostic details. Verification: 52 discovery, 71 core, 491 app no-default, strict no-default/all-features Clippy, fmt, locked workspace check, guardrails, diff check, and clean Serena diagnostics. Next allowed session is 16A.

## Session 16A transactional metadata Sort extension
- `discovery::metadata_sort` is a process-lifetime owner separate from Manual Add/visible jobs. At most one Sort is active. It captures app structural revision plus a domain immutable snapshot, uses the existing discovery executor only for missing/untrusted local metadata, then always submits key preparation/order to the neutral `bounded-work-executor` (one worker/one queued task currently). Natural and all-cached Sort still run CPU work off the UI callback; Natural performs no probe; URL performs no network work.
- Terminal success calls controller `preflight_canonical_sort`; all fallible app/domain checks precede Undo invalidation and the infallible combined commit. One actual metadata/reorder mutation yields one dirty/save publication; no-op yields none. Selected, active, pending and traversal identities remain bound to the same Item IDs.
- Cancel, structural edit/Clear, stale preflight, discovery/CPU error, or cooperative CPU cancellation never applies the prepared permutation. One metadata-only salvage batch revalidates Item ID+locator+expected fingerprint and first obtains a controller/domain prepared commit. Fatal invariant, app dirty revision exhaustion, or domain metadata revision exhaustion reject before Undo or queue mutation. Only a preflighted real change invalidates D40/D46 removal Undo, immediately before infallible apply; zero-change/fully stale/rejected salvage preserves Undo. Queue replacement and shutdown cancel the Sort independently of Manual Add/visible job ownership.
- Sort cancellation is typed and first-writer-wins (`Requested`, repeated request, already invalidated, stale job). Job IDs never wrap/reuse: after the one valid `u64::MAX` ID, start returns `JobIdExhausted`. The CPU task uses the neutral terminal notifier for event-driven AppWake after success, operation panic, and cancel-before-start, so no polling is required to observe terminal state.
- Verification: 5 neutral executor tests, 52 discovery, 75 core, 502 app no-default; focused all-cached/partial/error/cancel/Clear/URL/D44 0-1-N/mismatch/Undo rejection/identity/terminal wake/ID exhaustion/50k tests; strict no-default/all-feature Clippy, fmt, Rust 1.96 locked workspace check, guardrails, diff check and touched-file Serena diagnostics PASS. UI action/menu/progress rendering remains Session 19 scope; next allowed session is 17.


## Session 17 startup consumer (2026-07-16)
- CLI local сохраняет target-first policy и запускает sibling discovery только после exact Installed target. Restore queue/current никогда не создаёт discovery scope и не сканирует siblings. Полный startup contract: `mem:app-egui/startup-orchestration-s17`.
