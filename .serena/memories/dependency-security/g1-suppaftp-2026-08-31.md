# G1 suppaftp advisory closure (2026-08-31)

- Canonical dependency gate обнаружил blocking RUSTSEC-2026-0271 в `suppaftp 10.0.1`: FTP command CRLF injection.
- Root fix выполнен точным lockfile-only обновлением `suppaftp 10.0.1 -> 10.0.2`; manifests, first-party API и feature selection не менялись, advisory ignore/exception не добавлялись.
- `source-core` и `web-media-ftp` focused tests, strict Clippy, workspace tests/check, cargo-deny внутри `scripts/pre-pr-checks.sh` и release build прошли.
- Это дополняет, но не заменяет историческую closure `mem:dependency-security/aud-002-2026-08-23`.
