# AUD-002 dependency/security gate closure (2026-08-23)

- Отдельная read-only verification session воспроизвела `scripts/ci-checks.sh dependencies` exit 1 на исходном lockfile: blocking `event-listener 5.4.1` / RUSTSEC-2026-0221 и `webbrowser 1.2.1` / RUSTSEC-2026-0257.
- Обе зависимости были runtime-relevant для desktop graph: `app-egui -> rfd 0.15.4 -> ashpd 0.11.1 -> zbus 5.15.0 -> event-listener`, отдельная first-party ветка `app-egui -> desktop-integration -> zbus`, и `app-egui -> egui-winit 0.34.2 [links] -> webbrowser`.
- Existing constraints уже допускали исправленные версии; manifests, прямые зависимости, feature selection и Rust API не менялись.
- Security-only update выполнен точными командами `cargo update -p event-listener --precise 5.4.2` и `cargo update -p webbrowser --precise 1.2.2`. `Cargo.lock` больше не содержит уязвимые версии; `core-foundation 0.10.1`, использовавшаяся только старым webbrowser, удалена. Cargo также canonicalized/re-resolved существующие target-specific lock edges; manifests не менялись, `cargo metadata --locked` принимает graph.
- После фикса `scripts/ci-checks.sh dependencies` exit 0: blocking advisory stage clean; licenses/sources/bans и cargo-machete pass. RUSTSEC-2026-0150 `audiopus_sys 0.2.2` и RUSTSEC-2026-0192 `ttf-parser 0.25.1` остаются только в намеренно nonblocking visibility report, safe upgrade отсутствует.
- Feature invariants подтверждены locked trees: event-listener 5.4.2 остаётся в zbus/ashpd/rfd branches; webbrowser 1.2.2 остаётся активным через egui-winit default -> links.
- Verification PASS: `cargo test -p desktop-integration --locked` (25/25), `cargo test -p app-egui --locked` (950/950), `cargo check -p app-egui --locked`, `cargo +1.96.0 check --workspace --locked`, `cargo +1.92.0 check --workspace --locked`.
- Project-level automated test, реально запускающий системный browser через Unix `BROWSER`, отсутствует. Автоматическое доказательство link-opening boundary ограничено preserved feature tree, compilation и full app suite; реальный launch остаётся manual smoke.
- Source audit синхронизирован: `user/project_health_audit_2026-08-22.md`.