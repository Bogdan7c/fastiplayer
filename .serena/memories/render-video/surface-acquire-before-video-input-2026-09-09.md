# Surface acquisition before video input — issue #15

Owner approved session 02 design and Git workflow on 2026-09-09. Prerequisite #14 merged at 7dc707ce5bcef433791024ba260375fe80371aae. This is the narrow Ready-path correction, not scheduler or Busy redesign. Measured code ca657f2c passed the 18/18 final causal matrix and full pre-pr checks; final 60-attempt resource series still contains substantial 60 fps repeats, so it is NOT an accepted quality-fixed baseline. Public evidence: docs/benchmarks/ready-frame-fix-results.md and PR #16; exact private receipts/restoration/continuation belong in results/02.md. Do not assume all repeats are Busy or proceed automatically to later optimization stages.

`render-wgpu-shell/src/shell/frame_submission.rs` owns Renderer::acquire_frame and AcquiredRenderFrame::render. RenderFrameInput carries UI/screen/window metadata only; no video input before acquisition. The guard borrows Renderer exclusively, retaining surface, UI commands and texture delta until render or cancellation. Its read-only renderer() API lets app use existing GPU getters for DMA-BUF fallback without enabling resize/reconfigure during acquisition. Successful submission frees retired egui textures immediately after queue.submit and before poll/present, preserving the old integration order; it clears the free list so guard Drop only cleans up abandoned/failed transactions. Successful render consumes the surface exactly once. Surface consumer test carries a real textured UI mesh whose texture is set and retired in the same delta. Existing typed surface drop/recovery and fatal video error behavior is preserved. WGPU 29.0.3 Vulkan discard_texture is a no-op: abandoning an acquired frame/fatal video error still requires existing lifecycle reconfigure to recover all swapchain slots; the guard does not implicitly reconfigure.

`app-egui/frame_prepare/submit.rs::submit_render_frame` now acquires surface before prepare_video_frame, preserving app snapshot fences, nonblocking materialization, typed Busy/Missing/error/fallback and app submission accounting. Failed acquisition creates no video lease lookup or repeated-video accounting. Worker still selects eligible PTS and owns clock; decoder lease/GPU completion ownership and capacities unchanged. FrameSequenceStage now places SurfaceAcquire before MaterializerLookup; app video preparation is excluded from shell timing.

Functional checks: existing two separately run ignored app_instance::cadence_acceptance tests (real AppShell/worker/VA-API/swapchain) now require fresh Ready handoff; published-during-acquisition previously failed. Existing cadence_tests offscreen changing-pixel/shared-lease test remains snapshot characterization. New ignored `frame_prepare::shared_frame_materialization::cadence_tests::cadence_surface_tests::acquired_surface_preserves_video_ownership_and_recovers_after_drop_and_error` uses real shell with fake two-frame provider for absent video, abandoned acquisition, fatal video metadata, explicit existing reconfigure, subsequent Presented and exactly-once shared release after GPU completion; run alone on X11/Vulkan. No physical scanout claim or full real decoder release proof from fake sink. Commands/protocol: docs/benchmarks/frame-cadence-diagnostics.md. All validation outside sandbox, full scripts/pre-pr-checks.sh required. See `mem:testing/frame-cadence-diagnostics-2026-09-09` for prior causal evidence and historical limitations.