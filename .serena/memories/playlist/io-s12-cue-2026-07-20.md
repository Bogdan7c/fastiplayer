# Playlist I/O S12 — bounded CUE AUDIO parser (2026-07-20)

- `playlist-io::cue` — neutral byte-slice-only owner CUE AUDIO subset §D11. Public boundary: `CueParseRequest` + exact local `CueDocumentSource` + explicit `CueParserLimits` -> `parse_cue_document` -> bounded `CueDocument`. Parser не выполняет filesystem/network I/O, queue mutation, Item-ID allocation, player/open/probe work.
- Production layout: `cue/parser.rs` owns sequential FILE/TRACK/INDEX state and span/materialization rules; `cue/encoding.rs` owns UTF-8 optional BOM and UTF-16LE/BE mandatory-BOM decoding without code-page/BOM-less UTF-16 guessing; `cue/limits.rs` owns document/line/file/unknown/retained-text budgets; `cue/model.rs` owns public preview/provenance/error/eligibility vocabulary.
- Commands case-insensitive; TITLE/PERFORMER, FILE path/type token and retained unknown-command payload preserve case. FILE paths resolve against exact native CUE parent with `Path::join`; non-UTF native parent survives without `to_string_lossy` or canonicalization/I/O.
- Supported FILE profile is explicit `WAVE|AIFF|MP3|FLAC`; `BINARY`, `MOTOROLA` and unknown tokens fail typed. Только `TRACK .. AUDIO` создаёт draft; data modes fail typed. Первый TRACK может быть 01..99, следующие строго previous+1.
- INDEX grammar: optional 00, mandatory exactly one 01, then exact sequential 02..99. INDEX timestamps are nondecreasing across all tracks of one FILE section; MM:SS:FF validates SS<=59, FF<=74 and checked total-frame arithmetic. Exact 75-fps identity remains in `CueTimestamp`; `MediaTime` is the nanosecond playback projection.
- Каждый AUDIO track создаёт top-level ID-less `PlaylistSingleImportDraft` с `PlaylistPlaybackSpan`: start=INDEX01; same-FILE next INDEX01 is exclusive end (next pregap belongs to previous span); cross-FILE boundary and final track use EOF/open end. First-track HTOA INDEX00..01 retained in document but excluded from playback.
- `CueDocument` retains FILE sections, all indexes, metadata and bounded unknown commands. INDEX02..99 and unknown commands produce deterministic `CueExportIneligibility`; import still succeeds. Cue-specific retained diagnostics/sub-indexes are preview-owned and not embedded in the generic durable payload.
- No `playlist-core`, `playlist-state`, app or player code/API/schema changed. Existing core span/provenance/draft boundary was reused.
- Verification: 20 focused CUE tests + all 89 playlist-io tests PASS; focused strict Clippy PASS; Rust 1.96 workspace all-features check PASS; Rust 1.92 playlist-io all-targets check PASS; focused rustdoc `-Dwarnings`, fmt, diff check, refactor guardrails and Serena diagnostics PASS. Workspace-wide strict all-features Clippy remains blocked only by two pre-existing `app-egui` `large_enum_variant` findings in `state/strong_media_open{,/pending}.rs`, unrelated to S12.

## S14 continuation (2026-07-20)
S12 parser теперь прикрепляет к каждому Single exact `PlaylistCueTrackExportSemantics` и document-level eligibility. Unknown commands/sub-indexes по-прежнему импортируются/retained, но durable eligibility fail-closed запрещает exact CUE export. App `.cue` route читает документ в пределах `CueParserLimits` и вызывает authoritative `parse_cue_document`. Полный integration contract: `mem:app-egui/cue-integration-s14-2026-07-20`.
